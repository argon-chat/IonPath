//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

#pragma warning disable
#nullable enable


namespace TestContracts;

/// <summary>
/// Patch application over the wire.
/// </summary>
public sealed class Ion_PatchInteraction_ServiceExecutor(AsyncServiceScope scope) : IServiceExecutorRouter
{
    
    /// <summary>
    /// A patch in both argument and return position.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Apply_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __patch = IonFormatterStorage<IonPartial<PatchTarget>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Apply(__patch);
        
        IonFormatterStorage<IonPartial<PatchTarget>>.Write(writer, result);
    }
    /// <summary>
    /// An array of patches in, an optional patch out.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task ApplyMany_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __patches = IonFormatterStorage<IonPartial<PatchTarget>>.ReadArray(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.ApplyMany(__patches);
        
        IonFormatterStorage<IonPartial<PatchTarget>>.WriteNullable(writer, result);
    }
    /// <summary>
    /// `T~[]` in return position — `Array&lt;Partial&lt;PatchTarget&gt;&gt;`.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task ApplyAll_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __patches = IonFormatterStorage<IonPartial<PatchTarget>>.ReadArray(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.ApplyAll(__patches);
        
        IonFormatterStorage<IonPartial<PatchTarget>>.WriteArray(writer, result);
    }
    /// <summary>
    /// `T~[]?` in return position — `Maybe&lt;Array&lt;Partial&lt;PatchTarget&gt;&gt;&gt;`, the two collapsing
    /// wrappers stacked. An empty input returns null so the null branch is reachable.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task ApplySome_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __patches = IonFormatterStorage<IonPartial<PatchTarget>>.ReadArray(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.ApplySome(__patches);
        
        IonFormatterStorage<IonPartial<PatchTarget>>.WriteNullableArray(writer, result);
    }
    /// <summary>
    /// A patch beside a plain argument, to pin the argument count: the write path used
    /// to emit nothing for a partial while still declaring the full array length.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task ApplyTo_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 2;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __target = IonFormatterStorage<PatchTarget>.Read(reader);
        var __patch = IonFormatterStorage<IonPartial<PatchTarget>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.ApplyTo(__target, __patch);
        
        IonFormatterStorage<PatchTarget>.Write(writer, result);
    }
    /// <summary>
    /// Messages whose own fields are patches.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Rewrap_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IPatchInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __envelope = IonFormatterStorage<PatchEnvelope>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Rewrap(__envelope);
        
        IonFormatterStorage<PatchEnvelope>.Write(writer, result);
    }

    
    
    
        public Task RouteExecuteAsync(string methodName, CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        
        if (methodName.Equals("Apply", StringComparison.InvariantCultureIgnoreCase))
            return Apply_Execute(reader, writer, ct);
        if (methodName.Equals("ApplyMany", StringComparison.InvariantCultureIgnoreCase))
            return ApplyMany_Execute(reader, writer, ct);
        if (methodName.Equals("ApplyAll", StringComparison.InvariantCultureIgnoreCase))
            return ApplyAll_Execute(reader, writer, ct);
        if (methodName.Equals("ApplySome", StringComparison.InvariantCultureIgnoreCase))
            return ApplySome_Execute(reader, writer, ct);
        if (methodName.Equals("ApplyTo", StringComparison.InvariantCultureIgnoreCase))
            return ApplyTo_Execute(reader, writer, ct);
        if (methodName.Equals("Rewrap", StringComparison.InvariantCultureIgnoreCase))
            return Rewrap_Execute(reader, writer, ct);

        
        throw new InvalidOperationException("no method defined");
    }
    
    private static readonly string[] __allowedStreamingMethods = [
        
    ];
    
    public bool IsAllowInputStream(string methodName) => __allowedStreamingMethods.Contains(methodName);
}

