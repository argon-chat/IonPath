//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

#pragma warning disable
#nullable enable


namespace TestContracts;

/// <summary>
/// Deprecated service, to pin the marker on a service as well as on its methods.
/// </summary>
public sealed class Ion_LegacyCacheInteraction_ServiceExecutor(AsyncServiceScope scope) : IServiceExecutorRouter
{
    
    /// <summary>
    /// Nothing here is reachable; the service exists for its `@deprecated`.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Ping_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ILegacyCacheInteraction>();
    
        const int argumentSize = 0;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Ping();
        
        IonFormatterStorage<i4>.Write(writer, result);
    }

    
    
    
        public Task RouteExecuteAsync(string methodName, CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        
        if (methodName.Equals("Ping", StringComparison.InvariantCultureIgnoreCase))
            return Ping_Execute(reader, writer, ct);

        
        throw new InvalidOperationException("no method defined");
    }
    
    private static readonly string[] __allowedStreamingMethods = [
        
    ];
    
    public bool IsAllowInputStream(string methodName) => __allowedStreamingMethods.Contains(methodName);
}

/// <summary>
/// Cache control surface.
/// </summary>
public sealed class Ion_CacheInteraction_ServiceExecutor(AsyncServiceScope scope) : IServiceExecutorRouter
{
    
    /// <summary>
    /// `@deprecated` with both arguments, on a method.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Legacy_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICacheInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __id = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Legacy(__id);
        
        IonFormatterStorage<i4>.Write(writer, result);
    }
    /// <summary>
    /// `@deprecated` with only `reason`, written as a named argument — which the binder
    /// normalizes into `[null, "…"]`, i.e. a null in a *leading* slot that must survive.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Renamed_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICacheInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __id = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Renamed(__id);
        
        IonFormatterStorage<i4>.Write(writer, result);
    }
    /// <summary>
    /// `@deprecated` with no arguments, on a method.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Bare_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICacheInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __id = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Bare(__id);
        
        IonFormatterStorage<i4>.Write(writer, result);
    }
    /// <summary>
    /// A std attribute with a real C# counterpart beside a user declared one. `@deadline`
    /// is the case that used to emit `[deadline(30)]` and fail to resolve against
    /// `ion.runtime.DeadlineAttribute`.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Current_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICacheInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __id = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Current(__id);
        
        IonFormatterStorage<i4>.Write(writer, result);
    }
    /// <summary>
    /// A parameterless user attribute, and a std marker with no target-language form.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Ping_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICacheInteraction>();
    
        const int argumentSize = 0;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Ping();
        
        IonFormatterStorage<i4>.Write(writer, result);
    }

    
    
    
        public Task RouteExecuteAsync(string methodName, CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        
        if (methodName.Equals("Legacy", StringComparison.InvariantCultureIgnoreCase))
            return Legacy_Execute(reader, writer, ct);
        if (methodName.Equals("Renamed", StringComparison.InvariantCultureIgnoreCase))
            return Renamed_Execute(reader, writer, ct);
        if (methodName.Equals("Bare", StringComparison.InvariantCultureIgnoreCase))
            return Bare_Execute(reader, writer, ct);
        if (methodName.Equals("Current", StringComparison.InvariantCultureIgnoreCase))
            return Current_Execute(reader, writer, ct);
        if (methodName.Equals("Ping", StringComparison.InvariantCultureIgnoreCase))
            return Ping_Execute(reader, writer, ct);

        
        throw new InvalidOperationException("no method defined");
    }
    
    private static readonly string[] __allowedStreamingMethods = [
        
    ];
    
    public bool IsAllowInputStream(string methodName) => __allowedStreamingMethods.Contains(methodName);
}

