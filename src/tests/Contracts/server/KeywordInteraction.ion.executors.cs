//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

#pragma warning disable
#nullable enable


namespace TestContracts;

/// <summary>
/// Keyword names in method and argument position.
/// </summary>
public sealed class Ion_KeywordInteraction_ServiceExecutor(AsyncServiceScope scope) : IServiceExecutorRouter
{
    
    /// <summary>
    /// Reserved words as method *parameters*: a C# parameter, a Rust `async fn` argument
    /// and — the one that actually fails without a rename — a TypeScript parameter.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Echo_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IKeywordInteraction>();
    
        const int argumentSize = 3;
    
        var arraySize = reader.ReadStartMessage(argumentSize, "KeywordInteraction.Echo");
    
        var __class = IonFormatterStorage<string>.Read(reader);
        var __type = IonFormatterStorage<i4>.Read(reader);
        var __default = IonFormatterStorage<bool>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Echo(__class, __type, __default);
        
        IonFormatterStorage<string>.Write(writer, result);
    }
    /// <summary>
    /// A reserved word as the *method* name. The router still dispatches on the Ion
    /// spelling: the generated `methodName.Equals("lock", …)` is a string, not an
    /// identifier, and `nameof(@lock)` is `"lock"`.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task lock_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IKeywordInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartMessage(argumentSize, "KeywordInteraction.lock");
    
        var __value = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.@lock(__value);
        
        IonFormatterStorage<i4>.Write(writer, result);
    }
    /// <summary>
    /// The whole message over a real signature.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Roundtrip_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IKeywordInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartMessage(argumentSize, "KeywordInteraction.Roundtrip");
    
        var __fields = IonFormatterStorage<KeywordFields>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Roundtrip(__fields);
        
        IonFormatterStorage<KeywordFields>.Write(writer, result);
    }
    /// <summary>
    /// A patch in argument and return position, so the field-name keys are exercised
    /// end to end.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Patch_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IKeywordInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartMessage(argumentSize, "KeywordInteraction.Patch");
    
        var __patch = IonFormatterStorage<IonPartial<KeywordPatchTarget>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Patch(__patch);
        
        IonFormatterStorage<IonPartial<KeywordPatchTarget>>.Write(writer, result);
    }
    /// <summary>
    /// The union in both positions.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Pick_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<IKeywordInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartMessage(argumentSize, "KeywordInteraction.Pick");
    
        var __choice = IonFormatterStorage<IKeywordUnion>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Pick(__choice);
        
        IonFormatterStorage<IKeywordUnion>.Write(writer, result);
    }

    
    
    
        public Task RouteExecuteAsync(string methodName, CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        
        if (methodName.Equals("Echo", StringComparison.InvariantCultureIgnoreCase))
            return Echo_Execute(reader, writer, ct);
        if (methodName.Equals("lock", StringComparison.InvariantCultureIgnoreCase))
            return lock_Execute(reader, writer, ct);
        if (methodName.Equals("Roundtrip", StringComparison.InvariantCultureIgnoreCase))
            return Roundtrip_Execute(reader, writer, ct);
        if (methodName.Equals("Patch", StringComparison.InvariantCultureIgnoreCase))
            return Patch_Execute(reader, writer, ct);
        if (methodName.Equals("Pick", StringComparison.InvariantCultureIgnoreCase))
            return Pick_Execute(reader, writer, ct);

        
        throw new InvalidOperationException("no method defined");
    }
    
    private static readonly string[] __allowedStreamingMethods = [
        
    ];
    
    public bool IsAllowInputStream(string methodName) => __allowedStreamingMethods.Contains(methodName);
}

