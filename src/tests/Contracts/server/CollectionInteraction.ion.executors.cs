//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

#pragma warning disable
#nullable enable


namespace TestContracts;

/// <summary>
/// Round-trip surface: each container in argument and in return position.
///
/// Return position is where the three targets diverge most. C# has no
/// `CallAsyncWithFixedArray`, so the generated client checks `T[N]`'s length itself with the
/// runtime's own typed exception; TypeScript registers the whole fixed array under one
/// formatter name (`f4[16]`) and lets the ordinary nullable/plain call resolve it; Rust's
/// `[T; N]` needs nothing at all, because the const-generic `IonFormat` impl carries N.
/// </summary>
public sealed class Ion_CollectionInteraction_ServiceExecutor(AsyncServiceScope scope) : IServiceExecutorRouter
{
    
    /// <summary>
    /// `Map&lt;K,V&gt;` in and out. The integration test sends the same entries in two different
    /// insertion orders and asserts identical request bytes.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task CountByTag_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __tags = IonFormatterStorage<Dictionary<string, i4>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.CountByTag(__tags);
        
        IonFormatterStorage<Dictionary<string, i4>>.Write(writer, result);
    }
    /// <summary>
    /// `Set&lt;T&gt;` in and out — same insertion-order claim, plus tag 258 on the wire.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Dedup_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __ids = IonFormatterStorage<HashSet<i4>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Dedup(__ids);
        
        IonFormatterStorage<HashSet<i4>>.Write(writer, result);
    }
    /// <summary>
    /// `T[N]` in and out. A wrong length must be a typed error at both ends.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Rotate_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __coords = IonFormatterStorage<f4>.ReadFixedArray(reader, 16);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Rotate(__coords);
        
        IonFormatterStorage<f4>.WriteFixedArray(writer, result, 16);
    }
    /// <summary>
    /// `T[N]?` in and out. A null argument returns null, so the null branch is reachable.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task RotateMaybe_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __coords = IonFormatterStorage<f4>.ReadNullableFixedArray(reader, 16);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.RotateMaybe(__coords);
        
        IonFormatterStorage<f4>.WriteNullableFixedArray(writer, result, 16);
    }
    /// <summary>
    /// `Map&lt;K,V&gt;?` in and out, with a message value.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Lookup_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __members = reader.ReadNullable<Dictionary<string, Member>>();
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Lookup(__members);
        
        IonFormatterStorage<Dictionary<string, Member>>.WriteNullable(writer, result);
    }
    /// <summary>
    /// `Set&lt;T&gt;[]` in and out.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Regroup_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __groups = IonFormatterStorage<HashSet<i4>>.ReadArray(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Regroup(__groups);
        
        IonFormatterStorage<HashSet<i4>>.WriteArray(writer, result);
    }
    /// <summary>
    /// `Map&lt;K, T~&gt;` in and out.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Patch_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __patches = IonFormatterStorage<Dictionary<string, IonPartial<Doc>>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Patch(__patches);
        
        IonFormatterStorage<Dictionary<string, IonPartial<Doc>>>.Write(writer, result);
    }
    /// <summary>
    /// `Map&lt;K, T[]&gt;` in and out — the nested-array adapter over a real transport.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Roster_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __rosters = IonFormatterStorage<Dictionary<string, IonArray<Member>>>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Roster(__rosters);
        
        IonFormatterStorage<Dictionary<string, IonArray<Member>>>.Write(writer, result);
    }
    /// <summary>
    /// A container beside a plain argument, to pin the argument count: a write path that
    /// emits nothing for a container while still declaring the full array length produces
    /// malformed CBOR rather than a compile error.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Merge_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 2;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __baseline = IonFormatterStorage<Dictionary<string, i4>>.Read(reader);
        var __weight = IonFormatterStorage<i4>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Merge(__baseline, __weight);
        
        IonFormatterStorage<Dictionary<string, i4>>.Write(writer, result);
    }
    /// <summary>
    /// Every field shape in one message, over a real transport.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task Echo_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __shapes = IonFormatterStorage<ContainerShapes>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.Echo(__shapes);
        
        IonFormatterStorage<ContainerShapes>.Write(writer, result);
    }
    /// <summary>
    /// Every legal key type in one message, over a real transport.
    /// </summary>
    [GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
    public async Task EchoKeys_Execute(CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        var service = scope.ServiceProvider.GetRequiredService<ICollectionInteraction>();
    
        const int argumentSize = 1;
    
        var arraySize = reader.ReadStartArray() ?? throw new Exception("undefined len array not allowed");
    
        var __keys = IonFormatterStorage<KeyMatrix>.Read(reader);
    
        reader.ReadEndArrayAndSkip(arraySize - argumentSize);
    
        var result = await service.EchoKeys(__keys);
        
        IonFormatterStorage<KeyMatrix>.Write(writer, result);
    }

    
    
    
        public Task RouteExecuteAsync(string methodName, CborReader reader, CborWriter writer, CancellationToken ct = default)
    {
        
        if (methodName.Equals("CountByTag", StringComparison.InvariantCultureIgnoreCase))
            return CountByTag_Execute(reader, writer, ct);
        if (methodName.Equals("Dedup", StringComparison.InvariantCultureIgnoreCase))
            return Dedup_Execute(reader, writer, ct);
        if (methodName.Equals("Rotate", StringComparison.InvariantCultureIgnoreCase))
            return Rotate_Execute(reader, writer, ct);
        if (methodName.Equals("RotateMaybe", StringComparison.InvariantCultureIgnoreCase))
            return RotateMaybe_Execute(reader, writer, ct);
        if (methodName.Equals("Lookup", StringComparison.InvariantCultureIgnoreCase))
            return Lookup_Execute(reader, writer, ct);
        if (methodName.Equals("Regroup", StringComparison.InvariantCultureIgnoreCase))
            return Regroup_Execute(reader, writer, ct);
        if (methodName.Equals("Patch", StringComparison.InvariantCultureIgnoreCase))
            return Patch_Execute(reader, writer, ct);
        if (methodName.Equals("Roster", StringComparison.InvariantCultureIgnoreCase))
            return Roster_Execute(reader, writer, ct);
        if (methodName.Equals("Merge", StringComparison.InvariantCultureIgnoreCase))
            return Merge_Execute(reader, writer, ct);
        if (methodName.Equals("Echo", StringComparison.InvariantCultureIgnoreCase))
            return Echo_Execute(reader, writer, ct);
        if (methodName.Equals("EchoKeys", StringComparison.InvariantCultureIgnoreCase))
            return EchoKeys_Execute(reader, writer, ct);

        
        throw new InvalidOperationException("no method defined");
    }
    
    private static readonly string[] __allowedStreamingMethods = [
        
    ];
    
    public bool IsAllowInputStream(string methodName) => __allowedStreamingMethods.Contains(methodName);
}

