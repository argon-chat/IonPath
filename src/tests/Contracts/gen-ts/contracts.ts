//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------
import { 
  CborReader, 
  CborWriter, 
  
  DateOnly, 
  IonDateTime, 
  IonDecimal, 
  Duration, 
  TimeOnly, 
  Guid, 
  
  IonFormatterStorage,

  IonArray,
  IonMaybe,
  IonPartial,

  IIonService,
  IIonUnion,
  
  ServiceExecutor,
  IonClientContext,
  IonRequest,
  IonWsClient,
  IonInterceptor
} from "@argon-chat/ion.webcore";

type guid = Guid;
type timeonly = TimeOnly;
type duration = Duration;
// IonDateTime, never the deprecated `DateTimeOffset { date: Date; offsetMinutes }`
// shape: `Date` is millisecond-resolution, so it cannot hold the 100ns ticks the
// wire form carries, and the webcore "datetime" formatter now reads and writes
// IonDateTime — leaving the old alias here would be a live type mismatch, not just
// a lossy one.
type datetime = IonDateTime;
type decimal = IonDecimal;
type dateonly = DateOnly;

declare type bool = boolean;

declare type i1 = number;
declare type i2 = number;
declare type i4 = number;
declare type i8 = bigint;
declare type i16 = bigint;


declare type u1 = number;
declare type u2 = number;
declare type u4 = number;
declare type u8 = bigint;
declare type u16 = bigint;


declare type f2 = number;
declare type f4 = number;
declare type f8 = number;

/**
 * An instant.
 *
 * The alias exists to pin the *resolved* type name in each target. A field is emitted as
 * the primitive's own spelling, so only a typedef makes a generator write out what
 * `datetime` actually resolves to: `System.DateTimeOffset` in C# (never `System.DateTime`,
 * which has no offset field), `IonDateTime` in TypeScript, and
 * `chrono::DateTime<chrono::FixedOffset>` in Rust.
 */
export type Timestamp = datetime;

/**
 * An exact monetary amount.
 *
 * Same job as `Timestamp`, one primitive over: `System.Decimal`, `IonDecimal` and
 * `ion_rustcore::IonDecimal` — never a binary float.
 */
export type Money = decimal;

/**
 * A single vector component.
 *
 * A typedef is a transparent alias: on the wire this is an ordinary `f4`, and every
 * generated field below is emitted as `f4`. The alias exists only so hand written code
 * can name it.
 */
export type Scalar = number;

/**
 * A component count, used as a service operand.
 */
export type Rank = number;

/**
 * The request shape that `CacheProbe` replaced.
 *
 * `@deprecated` with no arguments: the bare form, and the one where every target has to
 * avoid emitting an empty argument list.
 *
 * @deprecated
 */
export interface LegacyRequest {
  /**
   * The pre-0.4 identifier.
   *
   * `@deprecated` with only `since`. On a field, because a C# positional record has to
   * route the attribute to the generated property rather than the constructor parameter.
   *
   * @deprecated since 0.4.
   */
  oldId: i4;
  /**
   * Not deprecated, so the neighbouring field proves the marker is per-member.
   */
  name: string;
};


/**
 * Carries `@Cache` on the declaration and on one of its fields.
 */
export interface CacheProbe {
  /**
   * Every argument written, including the trailing optional.
   */
  hits: i4;
  /**
   * The optional omitted and the array empty — the two shapes that used to emit
   * `[Cache(0,misses,)]` and an untyped `new[] { }`.
   */
  misses: i4;
  /**
   * Undecorated, to keep an unannotated field in the same record.
   */
  total: i4;
};


/**
 * Where a cached entry lives.
 *
 * `@deprecated` with both arguments, on the enum itself.
 *
 * @deprecated since 0.5: regions were replaced by explicit key parts
 */
export enum CacheRegion
{
  /**
   * Never produced any more. Bare `@deprecated` on an enum member.
   *
   * @deprecated
   */
  None = 0,
  /**
   * `@deprecated` with only `since`, on an enum member.
   *
   * @deprecated since 0.5.
   */
  Local = 1,
  /**
   * `@deprecated` with both arguments, on an enum member.
   *
   * @deprecated since 0.5: use Shared
   */
  Session = 2,
  /**
   * The surviving member.
   */
  Shared = 3,
}


/**
 * Plain message used as a `Map` value and a `Set`-adjacent element.
 */
export interface Member {
  id: guid;
  name: string;
};


/**
 * The `Partial<T>` target, so `Map<string, Doc~>` has something to patch.
 */
export interface Doc {
  title: string;
  revision: i4;
};


/**
 * Every key type ION0061 allows, one field each.
 *
 * The compiler's rule is: the ten integral builtins, `bool`, `duration`, `string`, `guid`,
 * and any enum. Nothing else — a float key would gain or lose entries on decode (`-0.0`
 * and `0.0` encode differently but compare equal, `NaN` does not compare equal to itself),
 * and `bytes`/`datetime` map to reference-equality types in at least one target.
 *
 * `duration` is deliberately absent despite being legal: `ion_rustcore::IonDuration` is
 * `#[derive(Debug, Clone, PartialEq)]` with no `Eq` and no `Hash`, so
 * `HashMap<IonDuration, i32>` does not compile. See the codegen report — the fix is one
 * derive in `packages/ion.rustcore/src/types.rs`, which is out of this change's scope.
 */
export interface KeyMatrix {
  /**
   * The narrow signed integrals. `i1` also pins the length-first rule at its most
   * visible: `-1` encodes as one byte (`20`) and `1000` as three (`1903e8`), so
   * canonical order puts `-1` first where a bytewise sort would put `1000` first.
   */
  byI1: Map<i1, i4>;
  byI2: Map<i2, i4>;
  byI4: Map<i4, i4>;
  byI8: Map<i8, i4>;
  /**
   * `i16`/`u16` are `Int128`/`UInt128` in C#, `bigint` in TypeScript and `i128`/`u128`
   * in Rust — three different key representations over one wire encoding.
   */
  byI16: Map<i16, i4>;
  byU1: Map<u1, i4>;
  byU2: Map<u2, i4>;
  byU4: Map<u4, i4>;
  byU8: Map<u8, i4>;
  byU16: Map<u16, i4>;
  /**
   * Two keys only, and the one case where the encoded key is a single byte in both arms.
   */
  byBool: Map<bool, i4>;
  /**
   * The common case, and the only key whose canonical order is *not* the same as the
   * target's natural string order: `"z"` (2 bytes encoded) sorts before `"aa"` (3).
   */
  byString: Map<string, i4>;
  /**
   * A 16-byte byte string on the wire; every target has a value-equal guid type.
   */
  byGuid: Map<guid, i4>;
  /**
   * The non-builtin key.
   */
  byEnum: Map<Tier, i4>;
};


/**
 * The three containers as ordinary message fields, plus every stacking.
 */
export interface ContainerShapes {
  /**
   * `Map<K,V>` — `Dictionary<string,i4>` / `Map<string,i4>` / `HashMap<String,i32>`.
   */
  tags: Map<string, i4>;
  /**
   * `Set<T>` — `HashSet<i4>` / `Set<i4>` / `HashSet<i32>`.
   */
  ids: Set<i4>;
  /**
   * `T[N]` — the one shape a target's type system can express only in Rust
   * (`[f32; 16]`); C# and TypeScript carry the length in the formatter call instead.
   */
  coords: IonArray<f4>;
  /**
   * `Map<K,V>?` — `Maybe<Map<…>>`. Null and empty are different values, and the
   * integration tests keep them apart.
   */
  membersByName: Map<string, Member> | null;
  /**
   * `Set<T>[]` — `Array<Set<i4>>`. A set inside an array is still one item to the
   * array codec, tag and all.
   */
  groups: IonArray<Set<i4>>;
  /**
   * The same shape written as an explicit generic. It must lock, and generate,
   * identically to `groups` above — the suffix form is sugar, not a second type.
   */
  layers: IonArray<Set<i4>>;
  /**
   * `T[N]?` — `Maybe<Array<f4, 16>>`. Two collapsing wrappers, read through a single
   * nullable-fixed call rather than a nullable of an array.
   */
  offsets: IonArray<f4> | null;
  /**
   * `Map<K, T~>` — a container over a sparse patch. The `Partial<Doc>` schema has to be
   * registered for this to resolve, and it is reached only through the Map's type
   * argument, never through a field of its own.
   */
  patches: Map<string, IonPartial<Doc>>;
  /**
   * `Map<K, T[]>` — the one nesting neither the C# nor the TypeScript runtime can serve
   * unaided. Everywhere else an array is reached with its *element* type
   * (`ReadArray<Member>`), so neither runtime registers a formatter for the array type
   * itself; a Map resolves its value by type/name alone. The generators emit the missing
   * adapter (`Ion_nested_array_Formatter<T>`, and a named `IonArray<Member>` registration)
   * for exactly this shape. Rust needs nothing — `Vec<T>: IonFormat` is blanket.
   */
  rosters: Map<string, IonArray<Member>>;
  /**
   * A `Set` nested in a `Map` value: the same adapter question, one container over.
   */
  cohorts: Map<Tier, Set<i4>>;
};


/**
 * Key type for the enum arm of `KeyMatrix`.
 *
 * An enum is the one non-builtin the compiler admits as a `Map` key (ION0061): an integral
 * base type and a closed set of named values, which every target can hash. It is also the
 * arm most likely to break on a target-by-target basis — a generated Rust enum has to
 * derive `Eq + Hash` for `HashMap<Tier, V>` to compile at all.
 */
export enum Tier
{
  Free = 0,
  Paid = 1,
  Trial = 2,
}


/**
 * One ledger entry: both new primitives under every modifier stacking.
 */
export interface LedgerEntry {
  /**
   * Bare `datetime`. Round-tripped with a non-UTC offset and a 100ns tick.
   */
  bookedAt: datetime;
  /**
   * Bare `decimal`. Round-tripped with a value binary floating point cannot represent.
   */
  amount: decimal;
  /**
   * `datetime?` — `System.Nullable<DateTimeOffset>`, `IonDateTime | null`,
   * `Option<chrono::DateTime<FixedOffset>>`. Both new primitives are value types in C#,
   * so this is the `NullableValue` path, not `NullableRef`.
   */
  settledAt: datetime | null;
  /**
   * `decimal?`.
   */
  fee: decimal | null;
  /**
   * `datetime[]`.
   */
  revisions: IonArray<datetime>;
  /**
   * `decimal[]`.
   */
  adjustments: IonArray<decimal>;
  /**
   * `decimal[]?` — the two collapsing wrappers stacked, read through a single
   * `ReadNullableArray` rather than a nullable of an array.
   */
  corrections: IonArray<decimal> | null;
  /**
   * A typedef over a builtin is transparent: this is an ordinary `datetime` on the wire,
   * and the generated field is spelled with the primitive, not with the alias.
   */
  openedAt: datetime;
  /**
   * The same, over `decimal`.
   */
  openingBalance: decimal;
};


/**
 * The `Partial<T>` target.
 *
 * Separate from `LedgerEntry` so the patch schema stays small enough to assert field by
 * field, and separate from `PartialInteraction.ion`'s `PatchTarget` so that the byte
 * comparable golden shape over there is not disturbed. What it adds over that one is a
 * value-typed *builtin struct* field: `IonPartialSchema<T>.Field<datetime>` and
 * `.NullableValue<datetime>` are different descriptors from the `i4`/`string` cases, and
 * only a `datetime`/`decimal` field reaches them.
 */
export interface LedgerPatch {
  /**
   * `.Field<datetime>` — clearing it must encode as `null`, not as `0001-01-01`.
   */
  bookedAt: datetime;
  /**
   * `.Field<decimal>` — clearing it must encode as `null`, not as `0`.
   */
  amount: decimal;
  /**
   * `.NullableValue<datetime>`: `DateTimeOffset` is a struct, so a nullable one is
   * `Nullable<DateTimeOffset>` and needs the value-typed descriptor.
   */
  settledAt: datetime | null;
  /**
   * `.Array<decimal>`.
   */
  adjustments: IonArray<decimal>;
};


/**
 * The message that gets patched.
 *
 * Deliberately the same shape as `GoldenPatchTarget` in
 * `/tests/golden/partial.golden.json` — a value-typed scalar, a float, a reference
 * scalar, an array field and an optional field — so a patch over it is byte-comparable
 * with the cross-runtime golden vectors. The array and the optional are the two shapes
 * the old reflection-derived formatter crashed on.
 */
export interface PatchTarget {
  /**
   * Value-typed scalar. Clearing it must encode as `null`, never as `0`.
   */
  n: i4;
  /**
   * Float. The R3 regression guard: `Removed()` here used to write the field's
   * default instead of `null`.
   */
  f: f4;
  /**
   * Reference-typed scalar.
   */
  s: string;
  /**
   * Array field.
   */
  items: IonArray<i4>;
  /**
   * Optional field. "Cleared" and "set to none" are the same patch.
   */
  note: string | null;
};


/**
 * Patches carried as ordinary message fields, in all four modifier stackings.
 *
 * `WrapModifiers` applies Partial innermost, then Array, then Maybe, so `PatchTarget~[]?`
 * is `Maybe<Array<Partial<PatchTarget>>>`.
 */
export interface PatchEnvelope {
  /**
   * `T~` — `IonPartial<PatchTarget>`.
   */
  one: IonPartial<PatchTarget>;
  /**
   * `T~[]` — `IonArray<IonPartial<PatchTarget>>`.
   */
  many: IonArray<IonPartial<PatchTarget>>;
  /**
   * `T~?` — a nullable `IonPartial<PatchTarget>`.
   */
  maybe: IonPartial<PatchTarget> | null;
  /**
   * `T~[]?` — a nullable `IonArray<IonPartial<PatchTarget>>`.
   */
  maybeMany: IonArray<IonPartial<PatchTarget>> | null;
};


export interface Vector {
  x: f4;
  y: f4;
  z: f4;
};


export interface VectorOfVector {
  x: Vector;
  y: Vector;
  z: Vector;
};


export interface VectorOfVectorOfVector {
  z: VectorOfVector;
  w: VectorOfVector;
};



IonFormatterStorage.register("LegacyRequest", {
  read(reader: CborReader): LegacyRequest {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const oldId = IonFormatterStorage.get<i4>('i4').read(reader);
    const name = IonFormatterStorage.get<string>('string').read(reader);
    reader.readEndArrayAndSkip(arraySize - 2);
    return { oldId, name };
  },
  write(writer: CborWriter, value: LegacyRequest): void {
    writer.writeStartArray(2);
    IonFormatterStorage.get<i4>('i4').write(writer, value.oldId);
    IonFormatterStorage.get<string>('string').write(writer, value.name);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("CacheProbe", {
  read(reader: CborReader): CacheProbe {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const hits = IonFormatterStorage.get<i4>('i4').read(reader);
    const misses = IonFormatterStorage.get<i4>('i4').read(reader);
    const total = IonFormatterStorage.get<i4>('i4').read(reader);
    reader.readEndArrayAndSkip(arraySize - 3);
    return { hits, misses, total };
  },
  write(writer: CborWriter, value: CacheProbe): void {
    writer.writeStartArray(3);
    IonFormatterStorage.get<i4>('i4').write(writer, value.hits);
    IonFormatterStorage.get<i4>('i4').write(writer, value.misses);
    IonFormatterStorage.get<i4>('i4').write(writer, value.total);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("CacheRegion", {
  read(reader: CborReader): CacheRegion {
    const num = (IonFormatterStorage.get<i4>('i4').read(reader))
    return CacheRegion[num] !== undefined ? num as CacheRegion : (() => {throw new Error('invalid enum type')})();
  },
  write(writer: CborWriter, value: CacheRegion): void {
    const casted: i4 = value;
    IonFormatterStorage.get<i4>('i4').write(writer, casted);
  }
});

IonFormatterStorage.register("Member", {
  read(reader: CborReader): Member {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const id = IonFormatterStorage.get<guid>('guid').read(reader);
    const name = IonFormatterStorage.get<string>('string').read(reader);
    reader.readEndArrayAndSkip(arraySize - 2);
    return { id, name };
  },
  write(writer: CborWriter, value: Member): void {
    writer.writeStartArray(2);
    IonFormatterStorage.get<guid>('guid').write(writer, value.id);
    IonFormatterStorage.get<string>('string').write(writer, value.name);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("Doc", {
  read(reader: CborReader): Doc {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const title = IonFormatterStorage.get<string>('string').read(reader);
    const revision = IonFormatterStorage.get<i4>('i4').read(reader);
    reader.readEndArrayAndSkip(arraySize - 2);
    return { title, revision };
  },
  write(writer: CborWriter, value: Doc): void {
    writer.writeStartArray(2);
    IonFormatterStorage.get<string>('string').write(writer, value.title);
    IonFormatterStorage.get<i4>('i4').write(writer, value.revision);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("KeyMatrix", {
  read(reader: CborReader): KeyMatrix {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const byI1 = IonFormatterStorage.get<Map<i1, i4>>('Map<i1, i4>').read(reader);
    const byI2 = IonFormatterStorage.get<Map<i2, i4>>('Map<i2, i4>').read(reader);
    const byI4 = IonFormatterStorage.get<Map<i4, i4>>('Map<i4, i4>').read(reader);
    const byI8 = IonFormatterStorage.get<Map<i8, i4>>('Map<i8, i4>').read(reader);
    const byI16 = IonFormatterStorage.get<Map<i16, i4>>('Map<i16, i4>').read(reader);
    const byU1 = IonFormatterStorage.get<Map<u1, i4>>('Map<u1, i4>').read(reader);
    const byU2 = IonFormatterStorage.get<Map<u2, i4>>('Map<u2, i4>').read(reader);
    const byU4 = IonFormatterStorage.get<Map<u4, i4>>('Map<u4, i4>').read(reader);
    const byU8 = IonFormatterStorage.get<Map<u8, i4>>('Map<u8, i4>').read(reader);
    const byU16 = IonFormatterStorage.get<Map<u16, i4>>('Map<u16, i4>').read(reader);
    const byBool = IonFormatterStorage.get<Map<bool, i4>>('Map<bool, i4>').read(reader);
    const byString = IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').read(reader);
    const byGuid = IonFormatterStorage.get<Map<guid, i4>>('Map<guid, i4>').read(reader);
    const byEnum = IonFormatterStorage.get<Map<Tier, i4>>('Map<Tier, i4>').read(reader);
    reader.readEndArrayAndSkip(arraySize - 14);
    return { byI1, byI2, byI4, byI8, byI16, byU1, byU2, byU4, byU8, byU16, byBool, byString, byGuid, byEnum };
  },
  write(writer: CborWriter, value: KeyMatrix): void {
    writer.writeStartArray(14);
    IonFormatterStorage.get<Map<i1, i4>>('Map<i1, i4>').write(writer, value.byI1);
    IonFormatterStorage.get<Map<i2, i4>>('Map<i2, i4>').write(writer, value.byI2);
    IonFormatterStorage.get<Map<i4, i4>>('Map<i4, i4>').write(writer, value.byI4);
    IonFormatterStorage.get<Map<i8, i4>>('Map<i8, i4>').write(writer, value.byI8);
    IonFormatterStorage.get<Map<i16, i4>>('Map<i16, i4>').write(writer, value.byI16);
    IonFormatterStorage.get<Map<u1, i4>>('Map<u1, i4>').write(writer, value.byU1);
    IonFormatterStorage.get<Map<u2, i4>>('Map<u2, i4>').write(writer, value.byU2);
    IonFormatterStorage.get<Map<u4, i4>>('Map<u4, i4>').write(writer, value.byU4);
    IonFormatterStorage.get<Map<u8, i4>>('Map<u8, i4>').write(writer, value.byU8);
    IonFormatterStorage.get<Map<u16, i4>>('Map<u16, i4>').write(writer, value.byU16);
    IonFormatterStorage.get<Map<bool, i4>>('Map<bool, i4>').write(writer, value.byBool);
    IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').write(writer, value.byString);
    IonFormatterStorage.get<Map<guid, i4>>('Map<guid, i4>').write(writer, value.byGuid);
    IonFormatterStorage.get<Map<Tier, i4>>('Map<Tier, i4>').write(writer, value.byEnum);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("ContainerShapes", {
  read(reader: CborReader): ContainerShapes {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const tags = IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').read(reader);
    const ids = IonFormatterStorage.get<Set<i4>>('Set<i4>').read(reader);
    const coords = IonFormatterStorage.readFixedArray<f4>(reader, 'f4', 16);
    const membersByName = IonFormatterStorage.readNullable<Map<string, Member>>(reader, 'Map<string, Member>');
    const groups = IonFormatterStorage.readArray<Set<i4>>(reader, 'Set<i4>');
    const layers = IonFormatterStorage.readArray<Set<i4>>(reader, 'Set<i4>');
    const offsets = IonFormatterStorage.readNullableFixedArray<f4>(reader, 'f4', 16);
    const patches = IonFormatterStorage.get<Map<string, IonPartial<Doc>>>('Map<string, IonPartial<Doc>>').read(reader);
    const rosters = IonFormatterStorage.get<Map<string, IonArray<Member>>>('Map<string, IonArray<Member>>').read(reader);
    const cohorts = IonFormatterStorage.get<Map<Tier, Set<i4>>>('Map<Tier, Set<i4>>').read(reader);
    reader.readEndArrayAndSkip(arraySize - 10);
    return { tags, ids, coords, membersByName, groups, layers, offsets, patches, rosters, cohorts };
  },
  write(writer: CborWriter, value: ContainerShapes): void {
    writer.writeStartArray(10);
    IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').write(writer, value.tags);
    IonFormatterStorage.get<Set<i4>>('Set<i4>').write(writer, value.ids);
    IonFormatterStorage.writeFixedArray<f4>(writer, value.coords, 'f4', 16);
    IonFormatterStorage.writeNullable<Map<string, Member>>(writer, value.membersByName, 'Map<string, Member>');
    IonFormatterStorage.writeArray<Set<i4>>(writer, value.groups, 'Set<i4>');
    IonFormatterStorage.writeArray<Set<i4>>(writer, value.layers, 'Set<i4>');
    IonFormatterStorage.writeNullableFixedArray<f4>(writer, value.offsets, 'f4', 16);
    IonFormatterStorage.get<Map<string, IonPartial<Doc>>>('Map<string, IonPartial<Doc>>').write(writer, value.patches);
    IonFormatterStorage.get<Map<string, IonArray<Member>>>('Map<string, IonArray<Member>>').write(writer, value.rosters);
    IonFormatterStorage.get<Map<Tier, Set<i4>>>('Map<Tier, Set<i4>>').write(writer, value.cohorts);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("Tier", {
  read(reader: CborReader): Tier {
    const num = (IonFormatterStorage.get<u1>('u1').read(reader))
    return Tier[num] !== undefined ? num as Tier : (() => {throw new Error('invalid enum type')})();
  },
  write(writer: CborWriter, value: Tier): void {
    const casted: u1 = value;
    IonFormatterStorage.get<u1>('u1').write(writer, casted);
  }
});

IonFormatterStorage.register("LedgerEntry", {
  read(reader: CborReader): LedgerEntry {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const bookedAt = IonFormatterStorage.get<datetime>('datetime').read(reader);
    const amount = IonFormatterStorage.get<decimal>('decimal').read(reader);
    const settledAt = IonFormatterStorage.readNullable<datetime>(reader, 'datetime');
    const fee = IonFormatterStorage.readNullable<decimal>(reader, 'decimal');
    const revisions = IonFormatterStorage.readArray<datetime>(reader, 'datetime');
    const adjustments = IonFormatterStorage.readArray<decimal>(reader, 'decimal');
    const corrections = IonFormatterStorage.readNullableArray<decimal>(reader, 'decimal');
    const openedAt = IonFormatterStorage.get<datetime>('datetime').read(reader);
    const openingBalance = IonFormatterStorage.get<decimal>('decimal').read(reader);
    reader.readEndArrayAndSkip(arraySize - 9);
    return { bookedAt, amount, settledAt, fee, revisions, adjustments, corrections, openedAt, openingBalance };
  },
  write(writer: CborWriter, value: LedgerEntry): void {
    writer.writeStartArray(9);
    IonFormatterStorage.get<datetime>('datetime').write(writer, value.bookedAt);
    IonFormatterStorage.get<decimal>('decimal').write(writer, value.amount);
    IonFormatterStorage.writeNullable<datetime>(writer, value.settledAt, 'datetime');
    IonFormatterStorage.writeNullable<decimal>(writer, value.fee, 'decimal');
    IonFormatterStorage.writeArray<datetime>(writer, value.revisions, 'datetime');
    IonFormatterStorage.writeArray<decimal>(writer, value.adjustments, 'decimal');
    IonFormatterStorage.writeNullableArray<decimal>(writer, value.corrections, 'decimal');
    IonFormatterStorage.get<datetime>('datetime').write(writer, value.openedAt);
    IonFormatterStorage.get<decimal>('decimal').write(writer, value.openingBalance);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("LedgerPatch", {
  read(reader: CborReader): LedgerPatch {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const bookedAt = IonFormatterStorage.get<datetime>('datetime').read(reader);
    const amount = IonFormatterStorage.get<decimal>('decimal').read(reader);
    const settledAt = IonFormatterStorage.readNullable<datetime>(reader, 'datetime');
    const adjustments = IonFormatterStorage.readArray<decimal>(reader, 'decimal');
    reader.readEndArrayAndSkip(arraySize - 4);
    return { bookedAt, amount, settledAt, adjustments };
  },
  write(writer: CborWriter, value: LedgerPatch): void {
    writer.writeStartArray(4);
    IonFormatterStorage.get<datetime>('datetime').write(writer, value.bookedAt);
    IonFormatterStorage.get<decimal>('decimal').write(writer, value.amount);
    IonFormatterStorage.writeNullable<datetime>(writer, value.settledAt, 'datetime');
    IonFormatterStorage.writeArray<decimal>(writer, value.adjustments, 'decimal');
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("PatchTarget", {
  read(reader: CborReader): PatchTarget {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const n = IonFormatterStorage.get<i4>('i4').read(reader);
    const f = IonFormatterStorage.get<f4>('f4').read(reader);
    const s = IonFormatterStorage.get<string>('string').read(reader);
    const items = IonFormatterStorage.readArray<i4>(reader, 'i4');
    const note = IonFormatterStorage.readNullable<string>(reader, 'string');
    reader.readEndArrayAndSkip(arraySize - 5);
    return { n, f, s, items, note };
  },
  write(writer: CborWriter, value: PatchTarget): void {
    writer.writeStartArray(5);
    IonFormatterStorage.get<i4>('i4').write(writer, value.n);
    IonFormatterStorage.get<f4>('f4').write(writer, value.f);
    IonFormatterStorage.get<string>('string').write(writer, value.s);
    IonFormatterStorage.writeArray<i4>(writer, value.items, 'i4');
    IonFormatterStorage.writeNullable<string>(writer, value.note, 'string');
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("PatchEnvelope", {
  read(reader: CborReader): PatchEnvelope {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const one = IonFormatterStorage.get<IonPartial<PatchTarget>>('IonPartial<PatchTarget>').read(reader);
    const many = IonFormatterStorage.readArray<IonPartial<PatchTarget>>(reader, 'IonPartial<PatchTarget>');
    const maybe = IonFormatterStorage.readNullable<IonPartial<PatchTarget>>(reader, 'IonPartial<PatchTarget>');
    const maybeMany = IonFormatterStorage.readNullableArray<IonPartial<PatchTarget>>(reader, 'IonPartial<PatchTarget>');
    reader.readEndArrayAndSkip(arraySize - 4);
    return { one, many, maybe, maybeMany };
  },
  write(writer: CborWriter, value: PatchEnvelope): void {
    writer.writeStartArray(4);
    IonFormatterStorage.get<IonPartial<PatchTarget>>('IonPartial<PatchTarget>').write(writer, value.one);
    IonFormatterStorage.writeArray<IonPartial<PatchTarget>>(writer, value.many, 'IonPartial<PatchTarget>');
    IonFormatterStorage.writeNullable<IonPartial<PatchTarget>>(writer, value.maybe, 'IonPartial<PatchTarget>');
    IonFormatterStorage.writeNullableArray<IonPartial<PatchTarget>>(writer, value.maybeMany, 'IonPartial<PatchTarget>');
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("Vector", {
  read(reader: CborReader): Vector {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const x = IonFormatterStorage.get<f4>('f4').read(reader);
    const y = IonFormatterStorage.get<f4>('f4').read(reader);
    const z = IonFormatterStorage.get<f4>('f4').read(reader);
    reader.readEndArrayAndSkip(arraySize - 3);
    return { x, y, z };
  },
  write(writer: CborWriter, value: Vector): void {
    writer.writeStartArray(3);
    IonFormatterStorage.get<f4>('f4').write(writer, value.x);
    IonFormatterStorage.get<f4>('f4').write(writer, value.y);
    IonFormatterStorage.get<f4>('f4').write(writer, value.z);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("VectorOfVector", {
  read(reader: CborReader): VectorOfVector {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const x = IonFormatterStorage.get<Vector>('Vector').read(reader);
    const y = IonFormatterStorage.get<Vector>('Vector').read(reader);
    const z = IonFormatterStorage.get<Vector>('Vector').read(reader);
    reader.readEndArrayAndSkip(arraySize - 3);
    return { x, y, z };
  },
  write(writer: CborWriter, value: VectorOfVector): void {
    writer.writeStartArray(3);
    IonFormatterStorage.get<Vector>('Vector').write(writer, value.x);
    IonFormatterStorage.get<Vector>('Vector').write(writer, value.y);
    IonFormatterStorage.get<Vector>('Vector').write(writer, value.z);
    writer.writeEndArray();
  }
});

IonFormatterStorage.register("VectorOfVectorOfVector", {
  read(reader: CborReader): VectorOfVectorOfVector {
    const arraySize = reader.readStartArray() ?? (() => { throw new Error("undefined len array not allowed") })();
    const z = IonFormatterStorage.get<VectorOfVector>('VectorOfVector').read(reader);
    const w = IonFormatterStorage.get<VectorOfVector>('VectorOfVector').read(reader);
    reader.readEndArrayAndSkip(arraySize - 2);
    return { z, w };
  },
  write(writer: CborWriter, value: VectorOfVectorOfVector): void {
    writer.writeStartArray(2);
    IonFormatterStorage.get<VectorOfVector>('VectorOfVector').write(writer, value.z);
    IonFormatterStorage.get<VectorOfVector>('VectorOfVector').write(writer, value.w);
    writer.writeEndArray();
  }
});


IonFormatterStorage.registerMap<i1, i4>("Map<i1, i4>", "i1", "i4");
IonFormatterStorage.registerMap<i2, i4>("Map<i2, i4>", "i2", "i4");
IonFormatterStorage.registerMap<i4, i4>("Map<i4, i4>", "i4", "i4");
IonFormatterStorage.registerMap<i8, i4>("Map<i8, i4>", "i8", "i4");
IonFormatterStorage.registerMap<i16, i4>("Map<i16, i4>", "i16", "i4");
IonFormatterStorage.registerMap<u1, i4>("Map<u1, i4>", "u1", "i4");
IonFormatterStorage.registerMap<u2, i4>("Map<u2, i4>", "u2", "i4");
IonFormatterStorage.registerMap<u4, i4>("Map<u4, i4>", "u4", "i4");
IonFormatterStorage.registerMap<u8, i4>("Map<u8, i4>", "u8", "i4");
IonFormatterStorage.registerMap<u16, i4>("Map<u16, i4>", "u16", "i4");
IonFormatterStorage.registerMap<bool, i4>("Map<bool, i4>", "bool", "i4");
IonFormatterStorage.registerMap<string, i4>("Map<string, i4>", "string", "i4");
IonFormatterStorage.registerMap<guid, i4>("Map<guid, i4>", "guid", "i4");
IonFormatterStorage.registerMap<Tier, i4>("Map<Tier, i4>", "Tier", "i4");
IonFormatterStorage.registerSet<i4>("Set<i4>", "i4");
IonFormatterStorage.registerFixedArray<f4>("f4[16]", "f4", 16);
IonFormatterStorage.registerMap<string, Member>("Map<string, Member>", "string", "Member");
IonFormatterStorage.registerMap<string, IonPartial<Doc>>("Map<string, IonPartial<Doc>>", "string", "IonPartial<Doc>");
IonFormatterStorage.register<IonArray<Member>>("IonArray<Member>", {
  read: (reader) => IonFormatterStorage.readArray<Member>(reader, "Member"),
  write: (writer, value) => IonFormatterStorage.writeArray<Member>(writer, value, "Member"),
});
IonFormatterStorage.registerMap<string, IonArray<Member>>("Map<string, IonArray<Member>>", "string", "IonArray<Member>");
IonFormatterStorage.registerMap<Tier, Set<i4>>("Map<Tier, Set<i4>>", "Tier", "Set<i4>");

IonFormatterStorage.registerPartial<Doc>("IonPartial<Doc>", [
  { name: "title", type: "string" },
  { name: "revision", type: "i4" },
]);

IonFormatterStorage.registerPartial<PatchTarget>("IonPartial<PatchTarget>", [
  { name: "n", type: "i4" },
  { name: "f", type: "f4" },
  { name: "s", type: "string" },
  { name: "items", type: "i4", kind: "array" },
  { name: "note", type: "string", kind: "nullable" },
]);

IonFormatterStorage.registerPartial<LedgerPatch>("IonPartial<LedgerPatch>", [
  { name: "bookedAt", type: "datetime" },
  { name: "amount", type: "decimal" },
  { name: "settledAt", type: "datetime", kind: "nullable" },
  { name: "adjustments", type: "decimal", kind: "array" },
]);



/**
 * Attribute emission fixtures.
 *
 * Nothing here is called by the integration tests: this module exists so that every
 * shape the code generators have to render for an attribute appears in checked-in
 * generated output, where a regression is a diff rather than a silent behaviour change.
 *
 * Two families are covered.
 *
 * * A **user declared** attribute, `@Cache`, with one parameter of each kind the
 *   attribute binder can produce — an integer, a string, a bool, an array, and a
 *   trailing optional — used with the optional written and with it omitted. Only C#
 *   has a general purpose annotation mechanism, so `@Cache` is expected to appear in
 *   the C# output alone.
 * * **`@deprecated`**, in all four argument shapes (none, `since` only, `reason` only,
 *   both), on a msg, a field, an enum, an enum member, a service and a method. This one
 *   is expected in every target, each in its own idiom: `[Obsolete]`, a `@deprecated`
 *   JSDoc tag, `#[deprecated]`, and a `// Deprecated:` doc paragraph.
 */
/**
 * Deprecated service, to pin the marker on a service as well as on its methods.
 *
 * @deprecated since 0.6: use CacheInteraction
 */
export interface ILegacyCacheInteraction extends IIonService
{
  /**
   * Nothing here is reachable; the service exists for its `@deprecated`.
   */
  Ping(): Promise<i4>;
}


/**
 * Cache control surface.
 */
export interface ICacheInteraction extends IIonService
{
  /**
   * `@deprecated` with both arguments, on a method.
   *
   * @deprecated since 0.6: use Current instead
   */
  Legacy(id: i4): Promise<i4>;
  /**
   * `@deprecated` with only `reason`, written as a named argument — which the binder
   * normalizes into `[null, "…"]`, i.e. a null in a *leading* slot that must survive.
   *
   * @deprecated renamed to Current
   */
  Renamed(id: i4): Promise<i4>;
  /**
   * `@deprecated` with no arguments, on a method.
   *
   * @deprecated
   */
  Bare(id: i4): Promise<i4>;
  /**
   * A std attribute with a real C# counterpart beside a user declared one. `@deadline`
   * is the case that used to emit `[deadline(30)]` and fail to resolve against
   * `ion.runtime.DeadlineAttribute`.
   */
  Current(id: i4): Promise<i4>;
  /**
   * A parameterless user attribute, and a std marker with no target-language form.
   */
  Ping(): Promise<i4>;
}




/**
 * `Map<K,V>`, `Set<T>` and the fixed-size array `T[N]` contracts.
 *
 * One file per shape would hide the interesting half. What has to hold here is that the
 * three containers behave like every other Ion type in *every* position a generator has
 * to spell — a message field, a method argument, a method return — and that they still
 * behave when stacked with the modifiers (`?`, `[]`, `~`) and with each other.
 *
 * What each container buys, and therefore what the integration tests assert:
 *
 * * `Map<K,V>` is a definite-length CBOR map whose keys are sorted in canonical
 *   RFC 8949 §4.2.1 order — byte length first, then lexicographically. The sort is the
 *   whole feature: a C# `Dictionary`, a JavaScript `Map` and a Rust `HashMap` have three
 *   different iteration orders, so without a total order the same logical map would
 *   produce three different byte strings.
 * * `Set<T>` is CBOR tag 258 wrapping a definite-length array, elements under the same
 *   ordering. The tag is what tells it apart from `Array<T>` on the wire; without it a
 *   captured payload would be ambiguous to anything that does not already hold the schema.
 * * `T[N]` is a definite-length array of exactly N, and a wrong length is a typed decode
 *   error naming both lengths — not a truncation, and not a silent pad.
 */
/**
 * Round-trip surface: each container in argument and in return position.
 *
 * Return position is where the three targets diverge most. C# has no
 * `CallAsyncWithFixedArray`, so the generated client checks `T[N]`'s length itself with the
 * runtime's own typed exception; TypeScript registers the whole fixed array under one
 * formatter name (`f4[16]`) and lets the ordinary nullable/plain call resolve it; Rust's
 * `[T; N]` needs nothing at all, because the const-generic `IonFormat` impl carries N.
 */
export interface ICollectionInteraction extends IIonService
{
  /**
   * `Map<K,V>` in and out. The integration test sends the same entries in two different
   * insertion orders and asserts identical request bytes.
   */
  CountByTag(tags: Map<string, i4>): Promise<Map<string, i4>>;
  /**
   * `Set<T>` in and out — same insertion-order claim, plus tag 258 on the wire.
   */
  Dedup(ids: Set<i4>): Promise<Set<i4>>;
  /**
   * `T[N]` in and out. A wrong length must be a typed error at both ends.
   */
  Rotate(coords: IonArray<f4>): Promise<IonArray<f4>>;
  /**
   * `T[N]?` in and out. A null argument returns null, so the null branch is reachable.
   */
  RotateMaybe(coords: IonArray<f4> | null): Promise<IonArray<f4> | null>;
  /**
   * `Map<K,V>?` in and out, with a message value.
   */
  Lookup(members: Map<string, Member> | null): Promise<Map<string, Member> | null>;
  /**
   * `Set<T>[]` in and out.
   */
  Regroup(groups: IonArray<Set<i4>>): Promise<IonArray<Set<i4>>>;
  /**
   * `Map<K, T~>` in and out.
   */
  Patch(patches: Map<string, IonPartial<Doc>>): Promise<Map<string, IonPartial<Doc>>>;
  /**
   * `Map<K, T[]>` in and out — the nested-array adapter over a real transport.
   */
  Roster(rosters: Map<string, IonArray<Member>>): Promise<Map<string, IonArray<Member>>>;
  /**
   * A container beside a plain argument, to pin the argument count: a write path that
   * emits nothing for a container while still declaring the full array length produces
   * malformed CBOR rather than a compile error.
   */
  Merge(baseline: Map<string, i4>, weight: i4): Promise<Map<string, i4>>;
  /**
   * Every field shape in one message, over a real transport.
   */
  Echo(shapes: ContainerShapes): Promise<ContainerShapes>;
  /**
   * Every legal key type in one message, over a real transport.
   */
  EchoKeys(keys: KeyMatrix): Promise<KeyMatrix>;
}




/**
 * Binary payload round-trip contracts.
 *
 * These services exist to exercise the `bytes` primitive across every method
 * shape the runtime supports: void return, single round-trip, and repeated
 * round-trips over the same connection.
 */
/**
 * Blob echo service used by the transport tests.
 *
 * Nothing here interprets the payload; the methods only prove that a byte
 * buffer survives serialization, transport and deserialization unchanged.
 */
export interface ITestBlobs extends IIonService
{
  /**
   * Accepts a payload and returns nothing. Exercises the void-return path.
   */
  Do(data: bytes): Promise<void>;
  /**
   * Echoes the payload back to the caller.
   */
  DoIt(data: bytes): Promise<bytes>;
  /**
   * Second echo overload. The duplicate methods let the tests issue several
   * distinct calls over one connection without reusing a method name.
   */
  DoIt2(data: bytes): Promise<bytes>;
  /**
   * Third echo overload. See DoIt2.
   */
  DoIt3(data: bytes): Promise<bytes>;
}




/**
 * `datetime` and `decimal` contracts.
 *
 * Both primitives exist for one reason — not silently rounding a value off — so both are
 * exercised here in every position a generator has to spell: bare, optional, array,
 * optional array, as a typedef target, inside a `Partial<T>` target, and in a service
 * signature (argument, return, and the service level bound operand).
 *
 * * `datetime` is CBOR tag 0 + RFC 3339 text, always with an explicit numeric offset and
 *   always with exactly seven fractional digits. It maps to `System.DateTimeOffset`,
 *   `IonDateTime` and `chrono::DateTime<chrono::FixedOffset>` — never to `System.DateTime`,
 *   which has no offset field and decodes `-05:00` back as UTC, and never to a JavaScript
 *   `Date`, which is millisecond resolution and cannot hold a 100ns tick.
 * * `decimal` is CBOR tag 4, `[exponent, mantissa]`. It maps to `System.Decimal`,
 *   `IonDecimal` and `ion_rustcore::IonDecimal` — never to `f8`, which cannot represent
 *   `0.1` at all.
 *
 * The integration tests round-trip a non-UTC offset, a sub-millisecond tick and a decimal
 * that `double` cannot hold, because those are the three values that tell a correct
 * mapping apart from one that merely compiles.
 */
/**
 * Round-trip surface for `datetime` and `decimal`.
 *
 * The service level operand is itself a `datetime`, so every generated method signature
 * carries one — that is the position that would have gone unnoticed if only fields were
 * covered.
 */
export interface ILedgerInteraction extends IIonService
{
  /**
   * `datetime` in both argument and return position. The one method whose result proves
   * the offset and the sub-millisecond tick survived the wire.
   */
  Echo(openedAt: datetime, at: datetime): Promise<datetime>;
  /**
   * `decimal` in both argument and return position.
   */
  EchoAmount(openedAt: datetime, amount: decimal): Promise<decimal>;
  /**
   * The whole message, so every field shape above goes over a real transport.
   */
  Roundtrip(openedAt: datetime, entry: LedgerEntry): Promise<LedgerEntry>;
  /**
   * Optional in argument and return position: `Maybe<datetime>` / `Maybe<decimal>`.
   * A null `amount` returns null, so the null branch is reachable.
   */
  EchoMaybe(openedAt: datetime, at: datetime | null, amount: decimal | null): Promise<decimal | null>;
  /**
   * Arrays of both, with an array return.
   */
  EchoMany(openedAt: datetime, instants: IonArray<datetime>, amounts: IonArray<decimal>): Promise<IonArray<datetime>>;
  /**
   * A `Partial<T>` whose target is made of the new primitives, in both positions.
   */
  Patch(openedAt: datetime, patch: IonPartial<LedgerPatch>): Promise<IonPartial<LedgerPatch>>;
}




/**
 * Arithmetic RPC surface used by the IonPath integration tests.
 *
 * Every service in this module is stateless: the left-hand operand is bound
 * once, as a service argument, and reused by every method call on that client.
 */
/**
 * Integer arithmetic over a fixed left-hand operand.
 *
 * The operand is supplied when the client is constructed, so calling Add(2)
 * on a client created with leftOperand = 40 yields 42.
 */
export interface IMathInteraction extends IIonService
{
  /**
   * Adds rightOperand to the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Add(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Multiplies the bound operand by rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Mul(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Subtracts rightOperand from the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Sub(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Divides the bound operand by rightOperand.
   *
   * Division by zero surfaces as an RPC fault, not as a return value.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Div(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Raises the bound operand to the power of rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Pow(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Raises the bound operand to each power in rightOperand, elementwise.
   *
   * The result array has the same length as the input array.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  PowArray(leftOperand: i4, rightOperand: IonArray<i4>): Promise<IonArray<i4>>;
  /**
   * Returns the absolute value of rightOperand, or null when it is null.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  ToPositive(leftOperand: i4, rightOperand: i4 | null): Promise<i4 | null>;
  /**
   * `i4[]?` in both argument and return position: null in, null out; otherwise the
   * elementwise power, same length as the input.
   *
   * The return is the shape the C# client used to emit `CallAsyncNullable<Array>` for —
   * `Maybe<Array<i4>>` collapsed to the bare word `Array` instead of naming the element
   * type. A value-typed element is the interesting half: `IonArray<i4>?` is a nullable
   * struct, so a reader that peels only one wrapper cannot even be coerced into compiling.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  PowArrayMaybe(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<i4> | null>;
  /**
   * The same return shape over a reference-typed element, to pin that the fix is not
   * specific to value types.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Spell(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<string> | null>;
}


/**
 * Deterministic pseudo-random streams seeded by the caller.
 */
export interface IRandomStreamInteraction extends IIonService
{
  /**
   * Streams i pseudo-random integers derived from the seed.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Integer(seed: i4, i: i4): AsyncIterable<i4>;
  /**
   * Bidirectional stream: every f4 pushed by the client is answered with a
   * pseudo-random f4 derived from the seed and that input.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Floats(seed: i4, i: AsyncIterable<f4>): AsyncIterable<f4>;
}




/**
 * `Partial<T>` ("T~") contracts.
 *
 * Exercises every shape the generators have to emit for a partial: as a message
 * field, as a method argument, as a method return, and under each of the four
 * modifier stackings (`T~`, `T~[]`, `T~?`, `T~[]?`).
 */
/**
 * Patch application over the wire.
 */
export interface IPatchInteraction extends IIonService
{
  /**
   * A patch in both argument and return position.
   */
  Apply(patch: IonPartial<PatchTarget>): Promise<IonPartial<PatchTarget>>;
  /**
   * An array of patches in, an optional patch out.
   */
  ApplyMany(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonPartial<PatchTarget> | null>;
  /**
   * `T~[]` in return position — `Array<Partial<PatchTarget>>`.
   */
  ApplyAll(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>>>;
  /**
   * `T~[]?` in return position — `Maybe<Array<Partial<PatchTarget>>>`, the two collapsing
   * wrappers stacked. An empty input returns null so the null branch is reachable.
   */
  ApplySome(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>> | null>;
  /**
   * A patch beside a plain argument, to pin the argument count: the write path used
   * to emit nothing for a partial while still declaring the full array length.
   */
  ApplyTo(target: PatchTarget, patch: IonPartial<PatchTarget>): Promise<PatchTarget>;
  /**
   * Messages whose own fields are patches.
   */
  Rewrap(envelope: PatchEnvelope): Promise<PatchEnvelope>;
}




export interface IVectorMathInteraction extends IIonService
{
  Abs(leftOperand: Vector): Promise<Vector>;
  Add(leftOperand: Vector, rightOperand: Vector): Promise<Vector>;
  AndNot(leftOperand: Vector, rightOperand: Vector): Promise<Vector>;
  Clamp(leftOperand: Vector, min: Vector, max: Vector): Promise<Vector>;
  /**
   * Exercises a typedef in both argument and return position.
   */
  Component(leftOperand: Vector, index: u1): Promise<f4>;
  Do(leftOperand: Vector): Promise<VectorOfVectorOfVector>;
  /**
   * `T[]` return over a message element, beside `Spread` below.
   */
  Repeat(leftOperand: Vector, count: u1): Promise<IonArray<Vector>>;
  /**
   * `T[]?` return over a message element: `count` of 0 returns null, not an empty array.
   *
   * The null case is the one that matters — an implementation that decodes the response as a
   * single `Vector` instead of a nullable array still "works" on a non-null payload of length
   * one, so only null-vs-empty-vs-populated together pin the wire shape.
   */
  Spread(leftOperand: Vector, count: u1): Promise<IonArray<Vector> | null>;
}




/**
 * Attribute emission fixtures.
 *
 * Nothing here is called by the integration tests: this module exists so that every
 * shape the code generators have to render for an attribute appears in checked-in
 * generated output, where a regression is a diff rather than a silent behaviour change.
 *
 * Two families are covered.
 *
 * * A **user declared** attribute, `@Cache`, with one parameter of each kind the
 *   attribute binder can produce — an integer, a string, a bool, an array, and a
 *   trailing optional — used with the optional written and with it omitted. Only C#
 *   has a general purpose annotation mechanism, so `@Cache` is expected to appear in
 *   the C# output alone.
 * * **`@deprecated`**, in all four argument shapes (none, `since` only, `reason` only,
 *   both), on a msg, a field, an enum, an enum member, a service and a method. This one
 *   is expected in every target, each in its own idiom: `[Obsolete]`, a `@deprecated`
 *   JSDoc tag, `#[deprecated]`, and a `// Deprecated:` doc paragraph.
 */
/**
 * Deprecated service, to pin the marker on a service as well as on its methods.
 *
 * @deprecated since 0.6: use CacheInteraction
 */
export interface ILegacyCacheInteraction extends IIonService
{
  /**
   * Nothing here is reachable; the service exists for its `@deprecated`.
   */
  Ping(): Promise<i4>;
}


/**
 * Cache control surface.
 */
export interface ICacheInteraction extends IIonService
{
  /**
   * `@deprecated` with both arguments, on a method.
   *
   * @deprecated since 0.6: use Current instead
   */
  Legacy(id: i4): Promise<i4>;
  /**
   * `@deprecated` with only `reason`, written as a named argument — which the binder
   * normalizes into `[null, "…"]`, i.e. a null in a *leading* slot that must survive.
   *
   * @deprecated renamed to Current
   */
  Renamed(id: i4): Promise<i4>;
  /**
   * `@deprecated` with no arguments, on a method.
   *
   * @deprecated
   */
  Bare(id: i4): Promise<i4>;
  /**
   * A std attribute with a real C# counterpart beside a user declared one. `@deadline`
   * is the case that used to emit `[deadline(30)]` and fail to resolve against
   * `ion.runtime.DeadlineAttribute`.
   */
  Current(id: i4): Promise<i4>;
  /**
   * A parameterless user attribute, and a std marker with no target-language form.
   */
  Ping(): Promise<i4>;
}




/**
 * `Map<K,V>`, `Set<T>` and the fixed-size array `T[N]` contracts.
 *
 * One file per shape would hide the interesting half. What has to hold here is that the
 * three containers behave like every other Ion type in *every* position a generator has
 * to spell — a message field, a method argument, a method return — and that they still
 * behave when stacked with the modifiers (`?`, `[]`, `~`) and with each other.
 *
 * What each container buys, and therefore what the integration tests assert:
 *
 * * `Map<K,V>` is a definite-length CBOR map whose keys are sorted in canonical
 *   RFC 8949 §4.2.1 order — byte length first, then lexicographically. The sort is the
 *   whole feature: a C# `Dictionary`, a JavaScript `Map` and a Rust `HashMap` have three
 *   different iteration orders, so without a total order the same logical map would
 *   produce three different byte strings.
 * * `Set<T>` is CBOR tag 258 wrapping a definite-length array, elements under the same
 *   ordering. The tag is what tells it apart from `Array<T>` on the wire; without it a
 *   captured payload would be ambiguous to anything that does not already hold the schema.
 * * `T[N]` is a definite-length array of exactly N, and a wrong length is a typed decode
 *   error naming both lengths — not a truncation, and not a silent pad.
 */
/**
 * Round-trip surface: each container in argument and in return position.
 *
 * Return position is where the three targets diverge most. C# has no
 * `CallAsyncWithFixedArray`, so the generated client checks `T[N]`'s length itself with the
 * runtime's own typed exception; TypeScript registers the whole fixed array under one
 * formatter name (`f4[16]`) and lets the ordinary nullable/plain call resolve it; Rust's
 * `[T; N]` needs nothing at all, because the const-generic `IonFormat` impl carries N.
 */
export interface ICollectionInteraction extends IIonService
{
  /**
   * `Map<K,V>` in and out. The integration test sends the same entries in two different
   * insertion orders and asserts identical request bytes.
   */
  CountByTag(tags: Map<string, i4>): Promise<Map<string, i4>>;
  /**
   * `Set<T>` in and out — same insertion-order claim, plus tag 258 on the wire.
   */
  Dedup(ids: Set<i4>): Promise<Set<i4>>;
  /**
   * `T[N]` in and out. A wrong length must be a typed error at both ends.
   */
  Rotate(coords: IonArray<f4>): Promise<IonArray<f4>>;
  /**
   * `T[N]?` in and out. A null argument returns null, so the null branch is reachable.
   */
  RotateMaybe(coords: IonArray<f4> | null): Promise<IonArray<f4> | null>;
  /**
   * `Map<K,V>?` in and out, with a message value.
   */
  Lookup(members: Map<string, Member> | null): Promise<Map<string, Member> | null>;
  /**
   * `Set<T>[]` in and out.
   */
  Regroup(groups: IonArray<Set<i4>>): Promise<IonArray<Set<i4>>>;
  /**
   * `Map<K, T~>` in and out.
   */
  Patch(patches: Map<string, IonPartial<Doc>>): Promise<Map<string, IonPartial<Doc>>>;
  /**
   * `Map<K, T[]>` in and out — the nested-array adapter over a real transport.
   */
  Roster(rosters: Map<string, IonArray<Member>>): Promise<Map<string, IonArray<Member>>>;
  /**
   * A container beside a plain argument, to pin the argument count: a write path that
   * emits nothing for a container while still declaring the full array length produces
   * malformed CBOR rather than a compile error.
   */
  Merge(baseline: Map<string, i4>, weight: i4): Promise<Map<string, i4>>;
  /**
   * Every field shape in one message, over a real transport.
   */
  Echo(shapes: ContainerShapes): Promise<ContainerShapes>;
  /**
   * Every legal key type in one message, over a real transport.
   */
  EchoKeys(keys: KeyMatrix): Promise<KeyMatrix>;
}




/**
 * Binary payload round-trip contracts.
 *
 * These services exist to exercise the `bytes` primitive across every method
 * shape the runtime supports: void return, single round-trip, and repeated
 * round-trips over the same connection.
 */
/**
 * Blob echo service used by the transport tests.
 *
 * Nothing here interprets the payload; the methods only prove that a byte
 * buffer survives serialization, transport and deserialization unchanged.
 */
export interface ITestBlobs extends IIonService
{
  /**
   * Accepts a payload and returns nothing. Exercises the void-return path.
   */
  Do(data: bytes): Promise<void>;
  /**
   * Echoes the payload back to the caller.
   */
  DoIt(data: bytes): Promise<bytes>;
  /**
   * Second echo overload. The duplicate methods let the tests issue several
   * distinct calls over one connection without reusing a method name.
   */
  DoIt2(data: bytes): Promise<bytes>;
  /**
   * Third echo overload. See DoIt2.
   */
  DoIt3(data: bytes): Promise<bytes>;
}




/**
 * `datetime` and `decimal` contracts.
 *
 * Both primitives exist for one reason — not silently rounding a value off — so both are
 * exercised here in every position a generator has to spell: bare, optional, array,
 * optional array, as a typedef target, inside a `Partial<T>` target, and in a service
 * signature (argument, return, and the service level bound operand).
 *
 * * `datetime` is CBOR tag 0 + RFC 3339 text, always with an explicit numeric offset and
 *   always with exactly seven fractional digits. It maps to `System.DateTimeOffset`,
 *   `IonDateTime` and `chrono::DateTime<chrono::FixedOffset>` — never to `System.DateTime`,
 *   which has no offset field and decodes `-05:00` back as UTC, and never to a JavaScript
 *   `Date`, which is millisecond resolution and cannot hold a 100ns tick.
 * * `decimal` is CBOR tag 4, `[exponent, mantissa]`. It maps to `System.Decimal`,
 *   `IonDecimal` and `ion_rustcore::IonDecimal` — never to `f8`, which cannot represent
 *   `0.1` at all.
 *
 * The integration tests round-trip a non-UTC offset, a sub-millisecond tick and a decimal
 * that `double` cannot hold, because those are the three values that tell a correct
 * mapping apart from one that merely compiles.
 */
/**
 * Round-trip surface for `datetime` and `decimal`.
 *
 * The service level operand is itself a `datetime`, so every generated method signature
 * carries one — that is the position that would have gone unnoticed if only fields were
 * covered.
 */
export interface ILedgerInteraction extends IIonService
{
  /**
   * `datetime` in both argument and return position. The one method whose result proves
   * the offset and the sub-millisecond tick survived the wire.
   */
  Echo(openedAt: datetime, at: datetime): Promise<datetime>;
  /**
   * `decimal` in both argument and return position.
   */
  EchoAmount(openedAt: datetime, amount: decimal): Promise<decimal>;
  /**
   * The whole message, so every field shape above goes over a real transport.
   */
  Roundtrip(openedAt: datetime, entry: LedgerEntry): Promise<LedgerEntry>;
  /**
   * Optional in argument and return position: `Maybe<datetime>` / `Maybe<decimal>`.
   * A null `amount` returns null, so the null branch is reachable.
   */
  EchoMaybe(openedAt: datetime, at: datetime | null, amount: decimal | null): Promise<decimal | null>;
  /**
   * Arrays of both, with an array return.
   */
  EchoMany(openedAt: datetime, instants: IonArray<datetime>, amounts: IonArray<decimal>): Promise<IonArray<datetime>>;
  /**
   * A `Partial<T>` whose target is made of the new primitives, in both positions.
   */
  Patch(openedAt: datetime, patch: IonPartial<LedgerPatch>): Promise<IonPartial<LedgerPatch>>;
}




/**
 * Arithmetic RPC surface used by the IonPath integration tests.
 *
 * Every service in this module is stateless: the left-hand operand is bound
 * once, as a service argument, and reused by every method call on that client.
 */
/**
 * Integer arithmetic over a fixed left-hand operand.
 *
 * The operand is supplied when the client is constructed, so calling Add(2)
 * on a client created with leftOperand = 40 yields 42.
 */
export interface IMathInteraction extends IIonService
{
  /**
   * Adds rightOperand to the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Add(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Multiplies the bound operand by rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Mul(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Subtracts rightOperand from the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Sub(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Divides the bound operand by rightOperand.
   *
   * Division by zero surfaces as an RPC fault, not as a return value.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Div(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Raises the bound operand to the power of rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Pow(leftOperand: i4, rightOperand: i4): Promise<i4>;
  /**
   * Raises the bound operand to each power in rightOperand, elementwise.
   *
   * The result array has the same length as the input array.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  PowArray(leftOperand: i4, rightOperand: IonArray<i4>): Promise<IonArray<i4>>;
  /**
   * Returns the absolute value of rightOperand, or null when it is null.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  ToPositive(leftOperand: i4, rightOperand: i4 | null): Promise<i4 | null>;
  /**
   * `i4[]?` in both argument and return position: null in, null out; otherwise the
   * elementwise power, same length as the input.
   *
   * The return is the shape the C# client used to emit `CallAsyncNullable<Array>` for —
   * `Maybe<Array<i4>>` collapsed to the bare word `Array` instead of naming the element
   * type. A value-typed element is the interesting half: `IonArray<i4>?` is a nullable
   * struct, so a reader that peels only one wrapper cannot even be coerced into compiling.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  PowArrayMaybe(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<i4> | null>;
  /**
   * The same return shape over a reference-typed element, to pin that the fix is not
   * specific to value types.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  Spell(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<string> | null>;
}


/**
 * Deterministic pseudo-random streams seeded by the caller.
 */
export interface IRandomStreamInteraction extends IIonService
{
  /**
   * Streams i pseudo-random integers derived from the seed.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Integer(seed: i4, i: i4): AsyncIterable<i4>;
  /**
   * Bidirectional stream: every f4 pushed by the client is answered with a
   * pseudo-random f4 derived from the seed and that input.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Floats(seed: i4, i: AsyncIterable<f4>): AsyncIterable<f4>;
}




/**
 * `Partial<T>` ("T~") contracts.
 *
 * Exercises every shape the generators have to emit for a partial: as a message
 * field, as a method argument, as a method return, and under each of the four
 * modifier stackings (`T~`, `T~[]`, `T~?`, `T~[]?`).
 */
/**
 * Patch application over the wire.
 */
export interface IPatchInteraction extends IIonService
{
  /**
   * A patch in both argument and return position.
   */
  Apply(patch: IonPartial<PatchTarget>): Promise<IonPartial<PatchTarget>>;
  /**
   * An array of patches in, an optional patch out.
   */
  ApplyMany(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonPartial<PatchTarget> | null>;
  /**
   * `T~[]` in return position — `Array<Partial<PatchTarget>>`.
   */
  ApplyAll(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>>>;
  /**
   * `T~[]?` in return position — `Maybe<Array<Partial<PatchTarget>>>`, the two collapsing
   * wrappers stacked. An empty input returns null so the null branch is reachable.
   */
  ApplySome(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>> | null>;
  /**
   * A patch beside a plain argument, to pin the argument count: the write path used
   * to emit nothing for a partial while still declaring the full array length.
   */
  ApplyTo(target: PatchTarget, patch: IonPartial<PatchTarget>): Promise<PatchTarget>;
  /**
   * Messages whose own fields are patches.
   */
  Rewrap(envelope: PatchEnvelope): Promise<PatchEnvelope>;
}




export interface IVectorMathInteraction extends IIonService
{
  Abs(leftOperand: Vector): Promise<Vector>;
  Add(leftOperand: Vector, rightOperand: Vector): Promise<Vector>;
  AndNot(leftOperand: Vector, rightOperand: Vector): Promise<Vector>;
  Clamp(leftOperand: Vector, min: Vector, max: Vector): Promise<Vector>;
  /**
   * Exercises a typedef in both argument and return position.
   */
  Component(leftOperand: Vector, index: u1): Promise<f4>;
  Do(leftOperand: Vector): Promise<VectorOfVectorOfVector>;
  /**
   * `T[]` return over a message element, beside `Spread` below.
   */
  Repeat(leftOperand: Vector, count: u1): Promise<IonArray<Vector>>;
  /**
   * `T[]?` return over a message element: `count` of 0 returns null, not an empty array.
   *
   * The null case is the one that matters — an implementation that decodes the response as a
   * single `Vector` instead of a nullable array still "works" on a non-null payload of length
   * one, so only null-vs-empty-vs-populated together pin the wire shape.
   */
  Spread(leftOperand: Vector, count: u1): Promise<IonArray<Vector> | null>;
}



//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

/**
 * Deprecated service, to pin the marker on a service as well as on its methods.
 *
 * @deprecated since 0.6: use CacheInteraction
 */
export class LegacyCacheInteraction_Executor extends ServiceExecutor<ILegacyCacheInteraction> implements ILegacyCacheInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * Nothing here is reachable; the service exists for its `@deprecated`.
   */
  async Ping(): Promise<i4> {
    const req = new IonRequest(this.ctx, "ILegacyCacheInteraction", "Ping");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(0);
          
    
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<ILegacyCacheInteraction>('LegacyCacheInteraction', LegacyCacheInteraction_Executor);

/**
 * Cache control surface.
 */
export class CacheInteraction_Executor extends ServiceExecutor<ICacheInteraction> implements ICacheInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * `@deprecated` with both arguments, on a method.
   *
   * @deprecated since 0.6: use Current instead
   */
  async Legacy(id: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "ICacheInteraction", "Legacy");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<i4>('i4').write(writer, id);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * `@deprecated` with only `reason`, written as a named argument — which the binder
   * normalizes into `[null, "…"]`, i.e. a null in a *leading* slot that must survive.
   *
   * @deprecated renamed to Current
   */
  async Renamed(id: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "ICacheInteraction", "Renamed");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<i4>('i4').write(writer, id);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * `@deprecated` with no arguments, on a method.
   *
   * @deprecated
   */
  async Bare(id: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "ICacheInteraction", "Bare");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<i4>('i4').write(writer, id);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * A std attribute with a real C# counterpart beside a user declared one. `@deadline`
   * is the case that used to emit `[deadline(30)]` and fail to resolve against
   * `ion.runtime.DeadlineAttribute`.
   */
  async Current(id: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "ICacheInteraction", "Current");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<i4>('i4').write(writer, id);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * A parameterless user attribute, and a std marker with no target-language form.
   */
  async Ping(): Promise<i4> {
    const req = new IonRequest(this.ctx, "ICacheInteraction", "Ping");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(0);
          
    
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<ICacheInteraction>('CacheInteraction', CacheInteraction_Executor);

/**
 * Round-trip surface: each container in argument and in return position.
 *
 * Return position is where the three targets diverge most. C# has no
 * `CallAsyncWithFixedArray`, so the generated client checks `T[N]`'s length itself with the
 * runtime's own typed exception; TypeScript registers the whole fixed array under one
 * formatter name (`f4[16]`) and lets the ordinary nullable/plain call resolve it; Rust's
 * `[T; N]` needs nothing at all, because the const-generic `IonFormat` impl carries N.
 */
export class CollectionInteraction_Executor extends ServiceExecutor<ICollectionInteraction> implements ICollectionInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * `Map<K,V>` in and out. The integration test sends the same entries in two different
   * insertion orders and asserts identical request bytes.
   */
  async CountByTag(tags: Map<string, i4>): Promise<Map<string, i4>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "CountByTag");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').write(writer, tags);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Map<string, i4>>("Map<string, i4>", writer.data, this.signal);
  }
  /**
   * `Set<T>` in and out — same insertion-order claim, plus tag 258 on the wire.
   */
  async Dedup(ids: Set<i4>): Promise<Set<i4>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Dedup");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Set<i4>>('Set<i4>').write(writer, ids);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Set<i4>>("Set<i4>", writer.data, this.signal);
  }
  /**
   * `T[N]` in and out. A wrong length must be a typed error at both ends.
   */
  async Rotate(coords: IonArray<f4>): Promise<IonArray<f4>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Rotate");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.writeFixedArray<f4>(writer, coords, 'f4', 16);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<f4>>("f4[16]", writer.data, this.signal);
  }
  /**
   * `T[N]?` in and out. A null argument returns null, so the null branch is reachable.
   */
  async RotateMaybe(coords: IonArray<f4> | null): Promise<IonArray<f4> | null> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "RotateMaybe");

    const writer = new CborWriter();

    writer.writeStartArray(1);

    IonFormatterStorage.writeNullableFixedArray<f4>(writer, coords, 'f4', 16);

    writer.writeEndArray();

    return await req.callAsyncNullableT<IonArray<f4>>("f4[16]", writer.data, this.signal);
  }
  /**
   * `Map<K,V>?` in and out, with a message value.
   */
  async Lookup(members: Map<string, Member> | null): Promise<Map<string, Member> | null> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Lookup");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.writeNullable<Map<string, Member>>(writer, members, 'Map<string, Member>');
      
    writer.writeEndArray();
          
    return await req.callAsyncNullableT<Map<string, Member>>("Map<string, Member>", writer.data, this.signal);
  }
  /**
   * `Set<T>[]` in and out.
   */
  async Regroup(groups: IonArray<Set<i4>>): Promise<IonArray<Set<i4>>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Regroup");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.writeArray<Set<i4>>(writer, groups, 'Set<i4>');
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<Set<i4>>>("IonArray<Set<i4>>", writer.data, this.signal);
  }
  /**
   * `Map<K, T~>` in and out.
   */
  async Patch(patches: Map<string, IonPartial<Doc>>): Promise<Map<string, IonPartial<Doc>>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Patch");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Map<string, IonPartial<Doc>>>('Map<string, IonPartial<Doc>>').write(writer, patches);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Map<string, IonPartial<Doc>>>("Map<string, IonPartial<Doc>>", writer.data, this.signal);
  }
  /**
   * `Map<K, T[]>` in and out — the nested-array adapter over a real transport.
   */
  async Roster(rosters: Map<string, IonArray<Member>>): Promise<Map<string, IonArray<Member>>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Roster");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Map<string, IonArray<Member>>>('Map<string, IonArray<Member>>').write(writer, rosters);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Map<string, IonArray<Member>>>("Map<string, IonArray<Member>>", writer.data, this.signal);
  }
  /**
   * A container beside a plain argument, to pin the argument count: a write path that
   * emits nothing for a container while still declaring the full array length produces
   * malformed CBOR rather than a compile error.
   */
  async Merge(baseline: Map<string, i4>, weight: i4): Promise<Map<string, i4>> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Merge");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<Map<string, i4>>('Map<string, i4>').write(writer, baseline);
    IonFormatterStorage.get<i4>('i4').write(writer, weight);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Map<string, i4>>("Map<string, i4>", writer.data, this.signal);
  }
  /**
   * Every field shape in one message, over a real transport.
   */
  async Echo(shapes: ContainerShapes): Promise<ContainerShapes> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "Echo");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<ContainerShapes>('ContainerShapes').write(writer, shapes);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<ContainerShapes>("ContainerShapes", writer.data, this.signal);
  }
  /**
   * Every legal key type in one message, over a real transport.
   */
  async EchoKeys(keys: KeyMatrix): Promise<KeyMatrix> {
    const req = new IonRequest(this.ctx, "ICollectionInteraction", "EchoKeys");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<KeyMatrix>('KeyMatrix').write(writer, keys);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<KeyMatrix>("KeyMatrix", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<ICollectionInteraction>('CollectionInteraction', CollectionInteraction_Executor);

/**
 * Blob echo service used by the transport tests.
 *
 * Nothing here interprets the payload; the methods only prove that a byte
 * buffer survives serialization, transport and deserialization unchanged.
 */
export class TestBlobs_Executor extends ServiceExecutor<ITestBlobs> implements ITestBlobs {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * Accepts a payload and returns nothing. Exercises the void-return path.
   */
  async Do(data: bytes): Promise<void> {
    const req = new IonRequest(this.ctx, "ITestBlobs", "Do");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<bytes>('bytes').write(writer, data);
      
    writer.writeEndArray();
          
    await req.callAsync(writer.data, this.signal);
  }
  /**
   * Echoes the payload back to the caller.
   */
  async DoIt(data: bytes): Promise<bytes> {
    const req = new IonRequest(this.ctx, "ITestBlobs", "DoIt");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<bytes>('bytes').write(writer, data);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<bytes>("bytes", writer.data, this.signal);
  }
  /**
   * Second echo overload. The duplicate methods let the tests issue several
   * distinct calls over one connection without reusing a method name.
   */
  async DoIt2(data: bytes): Promise<bytes> {
    const req = new IonRequest(this.ctx, "ITestBlobs", "DoIt2");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<bytes>('bytes').write(writer, data);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<bytes>("bytes", writer.data, this.signal);
  }
  /**
   * Third echo overload. See DoIt2.
   */
  async DoIt3(data: bytes): Promise<bytes> {
    const req = new IonRequest(this.ctx, "ITestBlobs", "DoIt3");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<bytes>('bytes').write(writer, data);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<bytes>("bytes", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<ITestBlobs>('TestBlobs', TestBlobs_Executor);

/**
 * Round-trip surface for `datetime` and `decimal`.
 *
 * The service level operand is itself a `datetime`, so every generated method signature
 * carries one — that is the position that would have gone unnoticed if only fields were
 * covered.
 */
export class LedgerInteraction_Executor extends ServiceExecutor<ILedgerInteraction> implements ILedgerInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * `datetime` in both argument and return position. The one method whose result proves
   * the offset and the sub-millisecond tick survived the wire.
   */
  async Echo(openedAt: datetime, at: datetime): Promise<datetime> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "Echo");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.get<datetime>('datetime').write(writer, at);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<datetime>("datetime", writer.data, this.signal);
  }
  /**
   * `decimal` in both argument and return position.
   */
  async EchoAmount(openedAt: datetime, amount: decimal): Promise<decimal> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "EchoAmount");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.get<decimal>('decimal').write(writer, amount);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<decimal>("decimal", writer.data, this.signal);
  }
  /**
   * The whole message, so every field shape above goes over a real transport.
   */
  async Roundtrip(openedAt: datetime, entry: LedgerEntry): Promise<LedgerEntry> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "Roundtrip");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.get<LedgerEntry>('LedgerEntry').write(writer, entry);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<LedgerEntry>("LedgerEntry", writer.data, this.signal);
  }
  /**
   * Optional in argument and return position: `Maybe<datetime>` / `Maybe<decimal>`.
   * A null `amount` returns null, so the null branch is reachable.
   */
  async EchoMaybe(openedAt: datetime, at: datetime | null, amount: decimal | null): Promise<decimal | null> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "EchoMaybe");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(3);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.writeNullable<datetime>(writer, at, 'datetime');
    IonFormatterStorage.writeNullable<decimal>(writer, amount, 'decimal');
      
    writer.writeEndArray();
          
    return await req.callAsyncNullableT<decimal>("decimal", writer.data, this.signal);
  }
  /**
   * Arrays of both, with an array return.
   */
  async EchoMany(openedAt: datetime, instants: IonArray<datetime>, amounts: IonArray<decimal>): Promise<IonArray<datetime>> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "EchoMany");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(3);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.writeArray<datetime>(writer, instants, 'datetime');
    IonFormatterStorage.writeArray<decimal>(writer, amounts, 'decimal');
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<datetime>>("IonArray<datetime>", writer.data, this.signal);
  }
  /**
   * A `Partial<T>` whose target is made of the new primitives, in both positions.
   */
  async Patch(openedAt: datetime, patch: IonPartial<LedgerPatch>): Promise<IonPartial<LedgerPatch>> {
    const req = new IonRequest(this.ctx, "ILedgerInteraction", "Patch");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<datetime>('datetime').write(writer, openedAt);
    IonFormatterStorage.get<IonPartial<LedgerPatch>>('IonPartial<LedgerPatch>').write(writer, patch);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonPartial<LedgerPatch>>("IonPartial<LedgerPatch>", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<ILedgerInteraction>('LedgerInteraction', LedgerInteraction_Executor);

/**
 * Integer arithmetic over a fixed left-hand operand.
 *
 * The operand is supplied when the client is constructed, so calling Add(2)
 * on a client created with leftOperand = 40 yields 42.
 */
export class MathInteraction_Executor extends ServiceExecutor<IMathInteraction> implements IMathInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * Adds rightOperand to the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Add(leftOperand: i4, rightOperand: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Add");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.get<i4>('i4').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * Multiplies the bound operand by rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Mul(leftOperand: i4, rightOperand: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Mul");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.get<i4>('i4').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * Subtracts rightOperand from the bound operand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Sub(leftOperand: i4, rightOperand: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Sub");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.get<i4>('i4').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * Divides the bound operand by rightOperand.
   *
   * Division by zero surfaces as an RPC fault, not as a return value.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Div(leftOperand: i4, rightOperand: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Div");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.get<i4>('i4').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * Raises the bound operand to the power of rightOperand.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Pow(leftOperand: i4, rightOperand: i4): Promise<i4> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Pow");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.get<i4>('i4').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<i4>("i4", writer.data, this.signal);
  }
  /**
   * Raises the bound operand to each power in rightOperand, elementwise.
   *
   * The result array has the same length as the input array.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async PowArray(leftOperand: i4, rightOperand: IonArray<i4>): Promise<IonArray<i4>> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "PowArray");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.writeArray<i4>(writer, rightOperand, 'i4');
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<i4>>("IonArray<i4>", writer.data, this.signal);
  }
  /**
   * Returns the absolute value of rightOperand, or null when it is null.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async ToPositive(leftOperand: i4, rightOperand: i4 | null): Promise<i4 | null> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "ToPositive");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.writeNullable<i4>(writer, rightOperand, 'i4');
      
    writer.writeEndArray();
          
    return await req.callAsyncNullableT<i4>("i4", writer.data, this.signal);
  }
  /**
   * `i4[]?` in both argument and return position: null in, null out; otherwise the
   * elementwise power, same length as the input.
   *
   * The return is the shape the C# client used to emit `CallAsyncNullable<Array>` for —
   * `Maybe<Array<i4>>` collapsed to the bare word `Array` instead of naming the element
   * type. A value-typed element is the interesting half: `IonArray<i4>?` is a nullable
   * struct, so a reader that peels only one wrapper cannot even be coerced into compiling.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async PowArrayMaybe(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<i4> | null> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "PowArrayMaybe");

    const writer = new CborWriter();

    writer.writeStartArray(2);

    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.writeNullableArray<i4>(writer, rightOperand, 'i4');

    writer.writeEndArray();

    return await req.callAsyncNullableArrayT<i4>("i4", writer.data, this.signal);
  }
  /**
   * The same return shape over a reference-typed element, to pin that the fix is not
   * specific to value types.
   *
   * @param leftOperand The left-hand operand shared by every method on this service.
   */
  async Spell(leftOperand: i4, rightOperand: IonArray<i4> | null): Promise<IonArray<string> | null> {
    const req = new IonRequest(this.ctx, "IMathInteraction", "Spell");

    const writer = new CborWriter();

    writer.writeStartArray(2);

    IonFormatterStorage.get<i4>('i4').write(writer, leftOperand);
    IonFormatterStorage.writeNullableArray<i4>(writer, rightOperand, 'i4');

    writer.writeEndArray();

    return await req.callAsyncNullableArrayT<string>("string", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<IMathInteraction>('MathInteraction', MathInteraction_Executor);

/**
 * Deterministic pseudo-random streams seeded by the caller.
 */
export class RandomStreamInteraction_Executor extends ServiceExecutor<IRandomStreamInteraction> implements IRandomStreamInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * Streams i pseudo-random integers derived from the seed.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Integer(seed: i4, i: i4): AsyncIterable<i4> {
    const ws = new IonWsClient(this.ctx, "IRandomStreamInteraction", "Integer");
    
    const writer = new CborWriter();
    
    writer.writeStartArray(2);
    
    IonFormatterStorage.get<i4>('i4').write(writer, seed);
    IonFormatterStorage.get<i4>('i4').write(writer, i);
    
    writer.writeEndArray();
    
    return ws.callServerStreaming<i4>("i4", writer.data, this.signal);
  }
  /**
   * Bidirectional stream: every f4 pushed by the client is answered with a
   * pseudo-random f4 derived from the seed and that input.
   *
   * @param seed Seed for the underlying generator. The same seed always yields the
   *   same sequence, which is what makes the streaming tests reproducible.
   */
  Floats(seed: i4, inputStream: AsyncIterable<f4>): AsyncIterable<f4> {
    const ws = new IonWsClient(this.ctx, "IRandomStreamInteraction", "Floats");
    
    const writer = new CborWriter();
    
    writer.writeStartArray(1);
    
    IonFormatterStorage.get<i4>('i4').write(writer, seed);
    
    writer.writeEndArray();
    
    return ws.callServerStreamingFullDuplex<f4, f4>("f4", writer.data, inputStream, "f4", this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<IRandomStreamInteraction>('RandomStreamInteraction', RandomStreamInteraction_Executor);

/**
 * Patch application over the wire.
 */
export class PatchInteraction_Executor extends ServiceExecutor<IPatchInteraction> implements IPatchInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  /**
   * A patch in both argument and return position.
   */
  async Apply(patch: IonPartial<PatchTarget>): Promise<IonPartial<PatchTarget>> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "Apply");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<IonPartial<PatchTarget>>('IonPartial<PatchTarget>').write(writer, patch);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonPartial<PatchTarget>>("IonPartial<PatchTarget>", writer.data, this.signal);
  }
  /**
   * An array of patches in, an optional patch out.
   */
  async ApplyMany(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonPartial<PatchTarget> | null> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "ApplyMany");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.writeArray<IonPartial<PatchTarget>>(writer, patches, 'IonPartial<PatchTarget>');
      
    writer.writeEndArray();
          
    return await req.callAsyncNullableT<IonPartial<PatchTarget>>("IonPartial<PatchTarget>", writer.data, this.signal);
  }
  /**
   * `T~[]` in return position — `Array<Partial<PatchTarget>>`.
   */
  async ApplyAll(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>>> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "ApplyAll");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.writeArray<IonPartial<PatchTarget>>(writer, patches, 'IonPartial<PatchTarget>');
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<IonPartial<PatchTarget>>>("IonArray<IonPartial<PatchTarget>>", writer.data, this.signal);
  }
  /**
   * `T~[]?` in return position — `Maybe<Array<Partial<PatchTarget>>>`, the two collapsing
   * wrappers stacked. An empty input returns null so the null branch is reachable.
   */
  async ApplySome(patches: IonArray<IonPartial<PatchTarget>>): Promise<IonArray<IonPartial<PatchTarget>> | null> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "ApplySome");

    const writer = new CborWriter();

    writer.writeStartArray(1);

    IonFormatterStorage.writeArray<IonPartial<PatchTarget>>(writer, patches, 'IonPartial<PatchTarget>');

    writer.writeEndArray();

    return await req.callAsyncNullableArrayT<IonPartial<PatchTarget>>("IonPartial<PatchTarget>", writer.data, this.signal);
  }
  /**
   * A patch beside a plain argument, to pin the argument count: the write path used
   * to emit nothing for a partial while still declaring the full array length.
   */
  async ApplyTo(target: PatchTarget, patch: IonPartial<PatchTarget>): Promise<PatchTarget> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "ApplyTo");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<PatchTarget>('PatchTarget').write(writer, target);
    IonFormatterStorage.get<IonPartial<PatchTarget>>('IonPartial<PatchTarget>').write(writer, patch);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<PatchTarget>("PatchTarget", writer.data, this.signal);
  }
  /**
   * Messages whose own fields are patches.
   */
  async Rewrap(envelope: PatchEnvelope): Promise<PatchEnvelope> {
    const req = new IonRequest(this.ctx, "IPatchInteraction", "Rewrap");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<PatchEnvelope>('PatchEnvelope').write(writer, envelope);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<PatchEnvelope>("PatchEnvelope", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<IPatchInteraction>('PatchInteraction', PatchInteraction_Executor);

export class VectorMathInteraction_Executor extends ServiceExecutor<IVectorMathInteraction> implements IVectorMathInteraction {
  constructor(public ctx: IonClientContext, private signal: AbortSignal) {
      super();
  }

  
  async Abs(leftOperand: Vector): Promise<Vector> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Abs");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Vector>("Vector", writer.data, this.signal);
  }
  async Add(leftOperand: Vector, rightOperand: Vector): Promise<Vector> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Add");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<Vector>('Vector').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Vector>("Vector", writer.data, this.signal);
  }
  async AndNot(leftOperand: Vector, rightOperand: Vector): Promise<Vector> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "AndNot");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<Vector>('Vector').write(writer, rightOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Vector>("Vector", writer.data, this.signal);
  }
  async Clamp(leftOperand: Vector, min: Vector, max: Vector): Promise<Vector> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Clamp");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(3);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<Vector>('Vector').write(writer, min);
    IonFormatterStorage.get<Vector>('Vector').write(writer, max);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<Vector>("Vector", writer.data, this.signal);
  }
  /**
   * Exercises a typedef in both argument and return position.
   */
  async Component(leftOperand: Vector, index: u1): Promise<f4> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Component");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<u1>('u1').write(writer, index);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<f4>("f4", writer.data, this.signal);
  }
  async Do(leftOperand: Vector): Promise<VectorOfVectorOfVector> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Do");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(1);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<VectorOfVectorOfVector>("VectorOfVectorOfVector", writer.data, this.signal);
  }
  /**
   * `T[]` return over a message element, beside `Spread` below.
   */
  async Repeat(leftOperand: Vector, count: u1): Promise<IonArray<Vector>> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Repeat");
          
    const writer = new CborWriter();
      
    writer.writeStartArray(2);
          
    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<u1>('u1').write(writer, count);
      
    writer.writeEndArray();
          
    return await req.callAsyncT<IonArray<Vector>>("IonArray<Vector>", writer.data, this.signal);
  }
  /**
   * `T[]?` return over a message element: `count` of 0 returns null, not an empty array.
   *
   * The null case is the one that matters — an implementation that decodes the response as a
   * single `Vector` instead of a nullable array still "works" on a non-null payload of length
   * one, so only null-vs-empty-vs-populated together pin the wire shape.
   */
  async Spread(leftOperand: Vector, count: u1): Promise<IonArray<Vector> | null> {
    const req = new IonRequest(this.ctx, "IVectorMathInteraction", "Spread");

    const writer = new CborWriter();

    writer.writeStartArray(2);

    IonFormatterStorage.get<Vector>('Vector').write(writer, leftOperand);
    IonFormatterStorage.get<u1>('u1').write(writer, count);

    writer.writeEndArray();

    return await req.callAsyncNullableArrayT<Vector>("Vector", writer.data, this.signal);
  }

}

IonFormatterStorage.registerClientExecutor<IVectorMathInteraction>('VectorMathInteraction', VectorMathInteraction_Executor);


export function createClient(endpoint: string, interceptors: IonInterceptor[]) {
  const ctx = {
    baseUrl: endpoint,
    interceptors: interceptors
  } as IonClientContext;
  const controller = new AbortController();

  return new Proxy(
    {},
    {
      get(_target, propKey) {
        if (typeof propKey !== "string") return undefined;
        if (propKey === "LegacyCacheInteraction") return IonFormatterStorage.createExecutor("LegacyCacheInteraction", ctx, controller.signal);
        if (propKey === "CacheInteraction") return IonFormatterStorage.createExecutor("CacheInteraction", ctx, controller.signal);
        if (propKey === "CollectionInteraction") return IonFormatterStorage.createExecutor("CollectionInteraction", ctx, controller.signal);
        if (propKey === "TestBlobs") return IonFormatterStorage.createExecutor("TestBlobs", ctx, controller.signal);
        if (propKey === "LedgerInteraction") return IonFormatterStorage.createExecutor("LedgerInteraction", ctx, controller.signal);
        if (propKey === "MathInteraction") return IonFormatterStorage.createExecutor("MathInteraction", ctx, controller.signal);
        if (propKey === "RandomStreamInteraction") return IonFormatterStorage.createExecutor("RandomStreamInteraction", ctx, controller.signal);
        if (propKey === "PatchInteraction") return IonFormatterStorage.createExecutor("PatchInteraction", ctx, controller.signal);
        if (propKey === "VectorMathInteraction") return IonFormatterStorage.createExecutor("VectorMathInteraction", ctx, controller.signal);


        throw new Error(`${propKey} service is not defined`);
      },
    }
  ) as {
    LegacyCacheInteraction: ILegacyCacheInteraction;
    CacheInteraction: ICacheInteraction;
    CollectionInteraction: ICollectionInteraction;
    TestBlobs: ITestBlobs;
    LedgerInteraction: ILedgerInteraction;
    MathInteraction: IMathInteraction;
    RandomStreamInteraction: IRandomStreamInteraction;
    PatchInteraction: IPatchInteraction;
    VectorMathInteraction: IVectorMathInteraction;

  };
}
