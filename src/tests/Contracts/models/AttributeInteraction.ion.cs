//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a code generation tool.
//     Changes to this file may cause incorrect behavior and will be lost
//     if the code is regenerated.
//
//     Generator: IonPath Codegen
// </auto-generated>
//------------------------------------------------------------------------------

#pragma warning disable
#nullable enable


namespace TestContracts;
// Attribute emission fixtures.
//
// Nothing here is called by the integration tests: this module exists so that every
// shape the code generators have to render for an attribute appears in checked-in
// generated output, where a regression is a diff rather than a silent behaviour change.
//
// Two families are covered.
//
// * A **user declared** attribute, `@Cache`, with one parameter of each kind the
//   attribute binder can produce — an integer, a string, a bool, an array, and a
//   trailing optional — used with the optional written and with it omitted. Only C#
//   has a general purpose annotation mechanism, so `@Cache` is expected to appear in
//   the C# output alone.
// * **`@deprecated`**, in all four argument shapes (none, `since` only, `reason` only,
//   both), on a msg, a field, an enum, an enum member, a service and a method. This one
//   is expected in every target, each in its own idiom: `[Obsolete]`, a `@deprecated`
//   JSDoc tag, `#[deprecated]`, and a `// Deprecated:` doc paragraph.

/// <summary>
/// The request shape that `CacheProbe` replaced.
///
/// `@deprecated` with no arguments: the bare form, and the one where every target has to
/// avoid emitting an empty argument list.
/// </summary>
/// <param name="oldId">The pre-0.4 identifier.
///
/// `@deprecated` with only `since`. On a field, because a C# positional record has to
/// route the attribute to the generated property rather than the constructor parameter.</param>
/// <param name="name">Not deprecated, so the neighbouring field proves the marker is per-member.</param>
[Obsolete]
[GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
public sealed record LegacyRequest([property: Obsolete("Since 0.4.")] i4 oldId, string name);


/// <summary>
/// Carries `@Cache` on the declaration and on one of its fields.
/// </summary>
/// <param name="hits">Every argument written, including the trailing optional.</param>
/// <param name="misses">The optional omitted and the array empty — the two shapes that used to emit
/// `[Cache(0,misses,)]` and an untyped `new[] { }`.</param>
/// <param name="total">Undecorated, to keep an unannotated field in the same record.</param>
[Cache(30, "requests", true, new i4[] { 0 }, "written on a msg")]
[GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
public sealed record CacheProbe([property: Cache(5, "field", false, new i4[] { 0, 1 }, "written on a field")] i4 hits, [property: Cache(0, "misses", false, new i4[0])] i4 misses, i4 total);


/// <summary>
/// Where a cached entry lives.
///
/// `@deprecated` with both arguments, on the enum itself.
/// </summary>
[Obsolete("Since 0.5: regions were replaced by explicit key parts")]
[GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
public enum CacheRegion
{
    /// <summary>
    /// Never produced any more. Bare `@deprecated` on an enum member.
    /// </summary>
    [Obsolete]
    None = 0,
    /// <summary>
    /// `@deprecated` with only `since`, on an enum member.
    /// </summary>
    [Obsolete("Since 0.5.")]
    Local = 1,
    /// <summary>
    /// `@deprecated` with both arguments, on an enum member.
    /// </summary>
    [Obsolete("Since 0.5: use Shared")]
    Session = 2,
    /// <summary>
    /// The surviving member.
    /// </summary>
    Shared = 3,
}


/// <summary>
/// Deprecated service, to pin the marker on a service as well as on its methods.
/// </summary>
[Obsolete("Since 0.6: use CacheInteraction")]
[GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
public interface ILegacyCacheInteraction : IIonService
{
    /// <summary>
    /// Nothing here is reachable; the service exists for its `@deprecated`.
    /// </summary>
    Task<i4> Ping(CancellationToken ct = default);
}


/// <summary>
/// Cache control surface.
/// </summary>
[Cache(60, "service", true, new i4[] { 0 }, "written on a service")]
[GeneratedCodeAttribute("ionc", null), CompilerGeneratedAttribute]
public interface ICacheInteraction : IIonService
{
    /// <summary>
    /// `@deprecated` with both arguments, on a method.
    /// </summary>
    [Obsolete("Since 0.6: use Current instead")]
    Task<i4> Legacy(i4 id, CancellationToken ct = default);
    /// <summary>
    /// `@deprecated` with only `reason`, written as a named argument — which the binder
    /// normalizes into `[null, "…"]`, i.e. a null in a *leading* slot that must survive.
    /// </summary>
    [Obsolete("Renamed to Current")]
    Task<i4> Renamed(i4 id, CancellationToken ct = default);
    /// <summary>
    /// `@deprecated` with no arguments, on a method.
    /// </summary>
    [Obsolete]
    Task<i4> Bare(i4 id, CancellationToken ct = default);
    /// <summary>
    /// A std attribute with a real C# counterpart beside a user declared one. `@deadline`
    /// is the case that used to emit `[deadline(30)]` and fail to resolve against
    /// `ion.runtime.DeadlineAttribute`.
    /// </summary>
    [Cache(15, "method", true, new i4[] { 0 }, "written on a method")]
    [Deadline(30)]
    Task<i4> Current(i4 id, CancellationToken ct = default);
    /// <summary>
    /// A parameterless user attribute, and a std marker with no target-language form.
    /// </summary>
    [AllowAnonymous]
    Task<i4> Ping(CancellationToken ct = default);
}


/// <summary>
/// Marks a declaration as cacheable.
///
/// Exists to exercise argument rendering, not to be honoured by any runtime.
/// </summary>
/// <param name="duration">How long a cached entry stays fresh, in seconds.</param>
/// <param name="region">The cache partition to store the entry in.</param>
/// <param name="shared">Whether the entry may be served to an anonymous caller.</param>
/// <param name="keyParts">Which argument positions take part in the cache key. Exercises an array argument,
/// including the empty case, which C# cannot write without naming the element type.</param>
/// <param name="note">Free text note. Optional and trailing, so most use sites leave it off — which is
/// what puts an explicit `null` in the last slot of the bound argument list.</param>
[AttributeUsage(AttributeTargets.All)]
public sealed class CacheAttribute(i4 duration, string region, bool shared, i4[] keyParts, string? note = null) : System.Attribute
{
    /// <summary>
    /// How long a cached entry stays fresh, in seconds.
    /// </summary>
    public i4 Duration => duration;
    /// <summary>
    /// The cache partition to store the entry in.
    /// </summary>
    public string Region => region;
    /// <summary>
    /// Whether the entry may be served to an anonymous caller.
    /// </summary>
    public bool Shared => shared;
    /// <summary>
    /// Which argument positions take part in the cache key. Exercises an array argument,
    /// including the empty case, which C# cannot write without naming the element type.
    /// </summary>
    public i4[] KeyParts => keyParts;
    /// <summary>
    /// Free text note. Optional and trailing, so most use sites leave it off — which is
    /// what puts an explicit `null` in the last slot of the bound argument list.
    /// </summary>
    public string? Note => note;

}

/// <summary>
/// Takes no arguments at all, to pin the bare `[Name]` form against `[Name()]`.
/// </summary>
[AttributeUsage(AttributeTargets.All)]
public sealed class AllowAnonymousAttribute() : System.Attribute
{

}

